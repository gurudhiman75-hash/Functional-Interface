import { useEffect, useState } from 'react';
import { Link, useParams } from 'react-router-dom';
import { RefreshCw, RotateCcw, Search, ShoppingCart } from 'lucide-react';

import { PageHeader } from '@/components/shared/PageHeader';
import { StatusBadge } from '@/components/shared/StatusBadge';
import { showToast } from '@/components/shared/toast';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
import { getFirebaseAuth } from '@/integrations/firebase';

const apiBase = ((import.meta.env.VITE_API_URL as string | undefined)?.trim() || '/api').replace(/\/$/, '');
const money = (minor: number, currency: string) => new Intl.NumberFormat('en-US', { style: 'currency', currency }).format(Number(minor || 0) / 100);
const dateTime = (value: string) => new Date(value).toLocaleString('en-US');

async function request<T>(path: string, init?: RequestInit): Promise<T> {
  const user = getFirebaseAuth()?.currentUser;
  if (!user) throw new Error('Your administrator session has expired.');
  const response = await fetch(`${apiBase}${path}`, { ...init, headers: { Authorization: `Bearer ${await user.getIdToken()}`, 'Content-Type': 'application/json', ...(init?.headers || {}) } });
  const body = await response.json().catch(() => null) as ({ error?: string } & T) | null;
  if (!response.ok) throw new Error(body?.error || `Commerce request failed (${response.status}).`);
  if (!body) throw new Error('Commerce returned an empty response.');
  return body;
}

type OrderRow = { id: string; orderNumber: string; status: string; currency: string; totalMinor: number; refundedMinor?: number; createdAt: string; paidAt: string | null; email: string; displayName: string | null; itemCount: number; entitlementCount: number; paymentStatus: string | null; providerOrderId: string | null; providerPaymentId: string | null };
type OrdersResponse = { orders: OrderRow[] };
type RefundRow = { id: string; providerRefundId: string | null; status: string; amountMinor: number; reason: string; createdAt: string; processedAt: string | null };
type DetailResponse = { order: OrderRow & { subtotalMinor: number; discountMinor: number; taxMinor: number; pricingSnapshot: unknown }; items: Array<{ id: string; productCode: string; title: string; versionNumber: number; totalMinor: number }>; payments: Array<{ id: string; provider: string; status: string; amountMinor: number; currency: string; providerOrderId: string | null; providerPaymentId: string | null; failureCode: string | null; failureMessage: string | null }>; events: Array<{ id: string; eventType: string; providerEventId: string; signatureVerified: boolean; receivedAt: string; processedAt: string | null; processingError: string | null }>; entitlements: Array<{ id: string; status: string; startsAt: string; endsAt: string | null; revokedAt: string | null; revokeReason: string | null; grantSource: string; testCount: number }>; refunds: RefundRow[]; refundedMinor: number; refundableMinor: number; readOnly: boolean };

export function OrdersPaymentsWorkspacePage() {
  const [search, setSearch] = useState(''); const [appliedSearch, setAppliedSearch] = useState('');
  const [data, setData] = useState<OrdersResponse | null>(null); const [loading, setLoading] = useState(true);
  const load = async () => { setLoading(true); try { const params = new URLSearchParams(); if (appliedSearch) params.set('search', appliedSearch); setData(await request<OrdersResponse>(`/admin/commerce/orders?${params}`)); } catch (error) { showToast.error('Unable to load orders', error instanceof Error ? error.message : 'Request failed.'); } finally { setLoading(false); } };
  useEffect(() => { void load(); }, [appliedSearch]);
  return <div className="space-y-5">
    <PageHeader title="Orders & Payments" description="Canonical order ledger, frozen pricing evidence, provider reconciliation, refunds and entitlement consequences." icon={<ShoppingCart className="h-5 w-5" />} actions={<Button variant="outline" onClick={() => void load()} disabled={loading}><RefreshCw className={`mr-1.5 h-4 w-4 ${loading ? 'animate-spin' : ''}`} />Refresh</Button>} />
    <Card><CardContent className="flex gap-2 p-4"><Input value={search} onChange={(event) => setSearch(event.target.value)} onKeyDown={(event) => { if (event.key === 'Enter') setAppliedSearch(search.trim()); }} placeholder="Search order number, student email or name" /><Button onClick={() => setAppliedSearch(search.trim())}><Search className="mr-1.5 h-4 w-4" />Search</Button></CardContent></Card>
    <Card><CardHeader><CardTitle className="text-base">Canonical order ledger</CardTitle></CardHeader><CardContent className="p-0"><Table><TableHeader><TableRow><TableHead>Order</TableHead><TableHead>Student</TableHead><TableHead>Status</TableHead><TableHead>Payment</TableHead><TableHead className="text-right">Items</TableHead><TableHead className="text-right">Entitlements</TableHead><TableHead className="text-right">Total</TableHead></TableRow></TableHeader><TableBody>{data?.orders.length ? data.orders.map((row) => <TableRow key={row.id}><TableCell><Link className="font-medium hover:underline" to={`/commerce/orders/${row.id}`}>#{row.orderNumber}</Link><p className="text-xs text-muted-foreground">{dateTime(row.createdAt)}</p></TableCell><TableCell><p>{row.displayName || 'Student'}</p><p className="text-xs text-muted-foreground">{row.email}</p></TableCell><TableCell><StatusBadge tone={row.status === 'paid' ? 'success' : row.status === 'refunded' ? 'neutral' : row.status === 'payment_failed' ? 'destructive' : 'warning'}>{row.status}</StatusBadge></TableCell><TableCell>{row.paymentStatus || '—'}</TableCell><TableCell className="text-right">{row.itemCount}</TableCell><TableCell className="text-right">{row.entitlementCount}</TableCell><TableCell className="text-right">{money(row.totalMinor, row.currency)}{Number(row.refundedMinor || 0) > 0 && <p className="text-xs text-muted-foreground">{money(Number(row.refundedMinor), row.currency)} refunded</p>}</TableCell></TableRow>) : <TableRow><TableCell colSpan={7} className="py-12 text-center text-muted-foreground">{loading ? 'Loading canonical orders…' : 'No canonical orders match this search.'}</TableCell></TableRow>}</TableBody></Table></CardContent></Card>
  </div>;
}

export function OrderPaymentDetailPage() {
  const { orderId } = useParams(); const [data, setData] = useState<DetailResponse | null>(null); const [loading, setLoading] = useState(true);
  const [amount, setAmount] = useState(''); const [reason, setReason] = useState(''); const [submitting, setSubmitting] = useState(false);
  const load = async () => { if (!orderId) return; setLoading(true); try { const next = await request<DetailResponse>(`/admin/commerce/orders/${encodeURIComponent(orderId)}`); setData(next); setAmount(String(next.refundableMinor)); } catch (error) { showToast.error('Unable to load order', error instanceof Error ? error.message : 'Request failed.'); } finally { setLoading(false); } };
  useEffect(() => { void load(); }, [orderId]);
  const submitRefund = async () => { if (!orderId) return; setSubmitting(true); try { await request(`/admin/commerce/orders/${encodeURIComponent(orderId)}/refunds`, { method: 'POST', body: JSON.stringify({ amountMinor: Number(amount), reason }) }); showToast.success('Refund requested', 'The provider request is recorded. Final status follows the verified webhook.'); setReason(''); await load(); } catch (error) { showToast.error('Refund request failed', error instanceof Error ? error.message : 'Request failed.'); } finally { setSubmitting(false); } };
  if (loading && !data) return <div className="py-12 text-center text-sm text-muted-foreground">Loading canonical order evidence…</div>;
  if (!data) return <div className="py-12 text-center text-sm text-muted-foreground">Order evidence is unavailable.</div>;
  const refundable = data.refundableMinor > 0 && data.payments.some((p) => ['captured', 'partially_refunded'].includes(p.status));
  return <div className="space-y-5"><PageHeader title={`Order #${data.order.orderNumber}`} description={`${data.order.email} · ${data.order.status}`} icon={<ShoppingCart className="h-5 w-5" />} actions={<Button asChild variant="outline"><Link to="/commerce/orders">Back to orders</Link></Button>} />
    <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Total" value={money(data.order.totalMinor, data.order.currency)} /><Metric label="Refunded" value={money(data.refundedMinor, data.order.currency)} /><Metric label="Payment attempts" value={String(data.payments.length)} /><Metric label="Entitlements" value={String(data.entitlements.length)} /></div>
    {refundable && <Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><RotateCcw className="h-4 w-4" />Request refund</CardTitle></CardHeader><CardContent className="space-y-3"><p className="text-sm text-muted-foreground">Enter minor currency units. A full verified refund revokes active entitlements from this order; a partial refund preserves access pending manual review.</p><div className="grid gap-3 sm:grid-cols-2"><Input type="number" min="1" max={data.refundableMinor} value={amount} onChange={(e) => setAmount(e.target.value)} placeholder="Amount in minor units" /><Input value={reason} onChange={(e) => setReason(e.target.value)} placeholder="Refund reason" /></div><Button onClick={() => void submitRefund()} disabled={submitting || reason.trim().length < 8 || Number(amount) <= 0 || Number(amount) > data.refundableMinor}>{submitting ? 'Submitting…' : `Request ${money(Number(amount || 0), data.order.currency)} refund`}</Button></CardContent></Card>}
    <Card><CardHeader><CardTitle className="text-base">Refund evidence</CardTitle></CardHeader><CardContent className="space-y-2">{data.refunds.length ? data.refunds.map((refund) => <div key={refund.id} className="rounded-md border p-3"><p className="font-medium">{money(refund.amountMinor, data.order.currency)} · {refund.status}</p><p className="text-xs text-muted-foreground">{refund.reason} · Provider {refund.providerRefundId || 'pending'} · Requested {dateTime(refund.createdAt)}{refund.processedAt ? ` · Processed ${dateTime(refund.processedAt)}` : ''}</p></div>) : <p className="text-sm text-muted-foreground">No refund has been requested.</p>}</CardContent></Card>
    <Card><CardHeader><CardTitle className="text-base">Purchased package versions</CardTitle></CardHeader><CardContent className="space-y-2">{data.items.map((item) => <div key={item.id} className="rounded-md border p-3"><p className="font-medium">{item.title}</p><p className="text-xs text-muted-foreground">{item.productCode} · immutable version {item.versionNumber} · {money(item.totalMinor, data.order.currency)}</p></div>)}</CardContent></Card>
    <Card><CardHeader><CardTitle className="text-base">Payment attempts and provider events</CardTitle></CardHeader><CardContent className="space-y-3">{data.payments.map((payment) => <div key={payment.id} className="rounded-md border p-3"><p className="font-medium">{payment.provider} · {payment.status}</p><p className="text-xs text-muted-foreground">Order {payment.providerOrderId || 'not assigned'} · Payment {payment.providerPaymentId || 'not captured'} · {money(payment.amountMinor, payment.currency)}</p></div>)}{data.events.map((event) => <div key={event.id} className="rounded-md border p-3"><p className="font-medium">{event.eventType}</p><p className="text-xs text-muted-foreground">Verified {String(event.signatureVerified)} · Received {dateTime(event.receivedAt)} · {event.processedAt ? 'processed' : 'pending'}</p>{event.processingError && <p className="mt-1 text-xs text-destructive">{event.processingError}</p>}</div>)}</CardContent></Card>
    <Card><CardHeader><CardTitle className="text-base">Issued entitlements</CardTitle></CardHeader><CardContent className="space-y-2">{data.entitlements.length ? data.entitlements.map((entry) => <div key={entry.id} className="rounded-md border p-3"><p className="font-medium">{entry.status} · {entry.testCount} tests</p><p className="text-xs text-muted-foreground">{entry.grantSource} · starts {dateTime(entry.startsAt)} · ends {entry.endsAt ? dateTime(entry.endsAt) : 'no expiry'}{entry.revokeReason ? ` · ${entry.revokeReason}` : ''}</p></div>) : <p className="text-sm text-muted-foreground">No entitlement has been issued for this order.</p>}</CardContent></Card>
  </div>;
}
function Metric({ label, value }: { label: string; value: string }) { return <Card><CardContent className="p-4"><p className="text-xs text-muted-foreground">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p></CardContent></Card>; }
export default OrdersPaymentsWorkspacePage;
