import { strict as assert } from "node:assert";
import { COM003_ENGLISH_REVIEW_CORPUS_V16_2 } from "./com003-review-synthesis-v16-2";
import { COM003_HINDI_LOCALIZATION_V2_WAVE4, COM003_LOCALIZATION_V2_WAVE4_AUTHORITY, COM003_PUNJABI_LOCALIZATION_V2_WAVE4 } from "./com003-localization-v2-wave4";
const QLS=["COM-003-QL-015","COM-003-QL-016","COM-003-QL-017","COM-003-QL-018","COM-003-QL-019"];
const EN=COM003_ENGLISH_REVIEW_CORPUS_V16_2.filter(q=>QLS.includes(q.qlId)); const enById=new Map(EN.map(q=>[q.questionId,q]));
assert.equal(EN.length,60);assert.equal(COM003_LOCALIZATION_V2_WAVE4_AUTHORITY.localizedQuestionCount,120);assert.equal(COM003_HINDI_LOCALIZATION_V2_WAVE4.length,60);assert.equal(COM003_PUNJABI_LOCALIZATION_V2_WAVE4.length,60);
for(const [language,items] of [["hi",COM003_HINDI_LOCALIZATION_V2_WAVE4],["pa",COM003_PUNJABI_LOCALIZATION_V2_WAVE4]] as const){
 assert.equal(new Set(items.map(q=>q.localizationId)).size,60);assert.equal(new Set(items.map(q=>q.sourceQuestionId)).size,60);
 for(const item of items){const en=enById.get(item.sourceQuestionId);assert.ok(en);assert.equal(item.sourceEnglishAuthorityId,"COM-003-ENGLISH-FREEZE-V2");assert.equal(item.qlId,en.qlId);assert.equal(item.cpId,en.cpId);assert.equal(item.examSurfaceFamily,en.examSurfaceFamily);assert.equal(item.surfaceMode,en.surfaceMode);assert.equal(item.targetFactId,en.targetFactId);assert.equal(item.correctIndex,en.correctIndex);assert.deepEqual(item.sourceIds,en.sourceIds);assert.deepEqual(item.sourceFactIds,en.sourceFactIds);assert.equal(item.versionScoped,en.versionScoped);assert.equal(item.solverAuthority,en.solverAuthority);assert.equal(item.options.length,4);assert.equal(item.canonicalAnswer,item.options[item.correctIndex]);assert.ok(item.stem.length>=20);assert.ok(item.explanation.length>=25);assert.equal(item.localizationReviewOnly,true);assert.equal(item.localizationFrozen,false);assert.equal(item.runtimeRegistered,false);assert.equal(item.productionReleased,false);if(language==="hi"){assert.match(item.stem,/[ऀ-ॿ]/);assert.match(item.explanation,/[ऀ-ॿ]/);}else{assert.match(item.stem,/[਀-੿]/);assert.match(item.explanation,/[਀-੿]/);}assert.doesNotMatch(item.explanation,/सही उत्तर है|ਸਹੀ ਉੱਤਰ ਹੈ|canonical fact|दिए गए तथ्य के अनुसार|ਦਿੱਤੇ ਤੱਥ ਅਨੁਸਾਰ/i);}
 for(const ql of QLS){const xs=items.filter(q=>q.qlId===ql);assert.equal(xs.length,12);assert.equal(new Set(xs.map(q=>q.stem.trim().toLowerCase())).size,12,`${language}:${ql}:duplicate-stem`);}
}
const g=COM003_LOCALIZATION_V2_WAVE4_AUTHORITY.governance;assert.equal(g.localizationFrozen,false);assert.equal(g.questionStudioRuntimeAuthorized,false);assert.equal(g.questionBankWritesAuthorized,false);assert.equal(g.testEligibilityAuthorized,false);assert.equal(g.mockTestEligibilityAuthorized,false);assert.equal(g.automaticPublicationAuthorized,false);assert.equal(g.publiclyPublishable,false);assert.equal(g.productionReleased,false);
console.log("[COM003-LOCALIZATION-V2-WAVE4]",{english:60,hindi:60,punjabi:60,qls:QLS,governance:g});
