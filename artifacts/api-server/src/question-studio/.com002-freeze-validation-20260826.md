# COM-002 freeze validation trigger

This short-lived file exists only to trigger the branch-local Content Engine validation workflow against the current `feature/content-engine-foundation-v1` base. It must not be merged into the feature branch.
