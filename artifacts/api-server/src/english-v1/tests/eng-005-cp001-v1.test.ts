import assert from"node:assert/strict";import{ENG005_CP001_IDIOMS_V1,eng005Cp001PoolV1,type Eng005Difficulty}from"../chapters/idioms-phrases/ENG-005/CP001/eng-005-cp001-lexicon-v1";import{generateEng005Cp001QuestionV1}from"../chapters/idioms-phrases/ENG-005/CP001/eng-005-cp001-v1";
assert.equal(ENG005_CP001_IDIOMS_V1.length,180);assert.equal(new Set(ENG005_CP001_IDIOMS_V1.map(x=>x.phrase.toLowerCase())).size,180);assert.equal(new Set(ENG005_CP001_IDIOMS_V1.map(x=>x.category)).size,12);assert.equal(eng005Cp001PoolV1("easy").length,60);assert.equal(eng005Cp001PoolV1("medium").length,84);assert.equal(eng005Cp001PoolV1("hard").length,36);
for(const e of ENG005_CP001_IDIOMS_V1){for(const mode of["idiom-to-meaning","meaning-to-idiom"]as const){const q=generateEng005Cp001QuestionV1({seed:`entry:${e.id}:${mode}`,difficulty:e.difficulty,entryId:e.id,mode});assert.equal(q.options.length,4);assert.equal(new Set(q.options.map(x=>x.toLowerCase())).size,4);}}
const seen=new Set<string>(),modes=new Set<string>(),pos=[0,0,0,0];for(let i=0;i<10000;i++){const d=(["easy","medium","hard"]as const)[i%3]as Eng005Difficulty,q=generateEng005Cp001QuestionV1({seed:`eng005-soak:${i}`,difficulty:d});seen.add(q.metadata.entryId);modes.add(`${d}:${q.metadata.mode}`);pos[q.correctOptionIndex]++;assert.equal(new Set(q.options.map(x=>x.toLowerCase())).size,4);}assert.equal(seen.size,180);for(const d of["easy","medium","hard"]as const){assert.ok(modes.has(`${d}:idiom-to-meaning`));assert.ok(modes.has(`${d}:meaning-to-idiom`));}for(const n of pos)assert.ok(n>=2200&&n<=2800,`Answer-position imbalance: ${pos.join(",")}`);console.log("ENG-005 CP001 validation passed.",{entries:180,positions:pos});
