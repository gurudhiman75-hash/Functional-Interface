export { combinationCount, factorialCount, permutationCount, committeeCompositionCount, pairTogetherLinearCount, pairApartLinearCount, fixedPositionAssignmentCount, terminalClassCodeCount } from "../../../shared/counting";
export function countPrimesInclusive(lower: number, upper: number): number { let count = 0; for (let value = Math.max(2, lower); value <= upper; value += 1) { let prime = true; for (let divisor = 2; divisor * divisor <= value; divisor += 1) if (value % divisor === 0) { prime = false; break; } if (prime) count += 1; } return count; }
export function countCompositesInclusive(lower: number, upper: number): number { let count = 0; for (let value = Math.max(4, lower); value <= upper; value += 1) { for (let divisor = 2; divisor * divisor <= value; divisor += 1) if (value % divisor === 0) { count += 1; break; } } return count; }
