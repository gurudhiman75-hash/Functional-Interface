# Difficulty framework

Difficulty is allocated per CP from the design profile. Hard items increase event depth, dependence, conditioning or counting structure; they do not rely on large arithmetic. See `distribution-targets.library.json` for exact counts.
