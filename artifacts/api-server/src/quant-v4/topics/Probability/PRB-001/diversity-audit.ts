import { runPrb001Pipeline, listPrb001QuestionEntries, PRB_001_LIBRARIES } from "./index";
const entries=listPrb001QuestionEntries();let exactStemRepeats=0,parameterRepeats=0;for(const entry of entries){const questions=Array.from({length:12},(_,index)=>runPrb001Pipeline(entry.cpId as any,{questionLanguageId:entry.qlId,seed:`PRB-001:diversity:${entry.qlId}:${index}`}));exactStemRepeats+=12-new Set(questions.map((q)=>q.stem)).size;parameterRepeats+=12-new Set(questions.map((q)=>q.parameterFingerprint)).size;}if(parameterRepeats)throw new Error(`Parameter diversity repeats=${parameterRepeats}`);console.log(JSON.stringify({packageId:"PRB-001",qlCount:entries.length,seedsPerQl:12,exactStemRepeats,parameterRepeats}));
