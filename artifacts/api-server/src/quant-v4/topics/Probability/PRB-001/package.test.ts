import { runPrb001Pipeline, listPrb001QuestionEntries, PRB_001_LIBRARIES } from "./index";
function assert(condition:unknown,message:string):asserts condition{if(!condition)throw new Error(message);}
const entries=listPrb001QuestionEntries();let runs=0;for(const entry of entries){const seed=`PRB-001:forced:${entry.qlId}`;const first=runPrb001Pipeline(entry.cpId as any,{questionLanguageId:entry.qlId,seed}),second=runPrb001Pipeline(entry.cpId as any,{questionLanguageId:entry.qlId,seed});assert(first.validation.valid,`${entry.qlId} validation failed`);assert(first.independentVerification.matched,`${entry.qlId} independent verifier failed`);assert(JSON.stringify(first)===JSON.stringify(second),`${entry.qlId} is not deterministic`);runs+=2;}console.log(JSON.stringify({packageId:"PRB-001",qlCount:entries.length,deterministicRuns:runs}));
