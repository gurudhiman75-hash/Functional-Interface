# MEN-CP-012 — Permanent English Runtime Candidate V1

Authority: `MEN-CP012-PERMANENT-ENGLISH-RUNTIME-V1-CANDIDATE`

## Status

The 13 permanent reasoning identities `MEN-002-QL-150..MEN-002-QL-162` now have an executable permanent English **candidate runtime**.

This is not an English freeze and not product activation.

## Runtime source policy

The permanent runtime does not re-derive a second independent question engine. It routes each permanent QL through the already proved discovery/source pools:

```text
Wave 01 -> MEN-CP012-FOUNDATION-PRESENTATION-V2
Wave 02 -> MEN-CP012-DISCOVERY-WAVE-02-SAFE-RUNTIME-V2
Wave 03 -> MEN-CP012-SATURATION-WAVE-03-SAFE-V2
Wave 04 corrected cone-ratio source -> MEN-CP012-SOURCE-CORRECTIONS-V4-V1
```

`V3-SPHERE-TO-CONE-DIAMETER-HEIGHT-RATIO` is never routed through the semantically weaker Wave-03 safe presentation. The permanent runtime always uses the Wave-04 correction that gives cone height, recovers radius by square root, converts to diameter and then forms the requested ratio.

## Permanent teaching layer

Each QL adds a family-level:

- governing key rule;
- exam shortcut;
- misconception policy.

The actual worked steps remain source-state specific and preserve the numeric conservation equation from the underlying proved source.

## Machine proof target

```text
13 QLs × 128 deterministic forced-source states = 1,664 questions
```

The proof deliberately forces sources rather than relying on probabilistic routing so that:

- all 42 permanent source mappings are exercised;
- every QL reaches A/B/C/D;
- every source verification remains valid;
- option uniqueness and displayed-answer parity are rechecked at the permanent layer;
- the Wave-04 cone-ratio correction is exercised explicitly;
- English/product lifecycle flags remain closed.

## Human review design

The human artifact is **source-complete rather than artificially fixed-size**.

For each QL:

1. every declared source representation must appear at least once;
2. A/B/C/D must all appear;
3. selected stems must be distinct;
4. additional source-heavy families may therefore have more records than single-source families.

This produces **59 review records** across the 13 QLs and exposes all 42 sources without padding low-entropy families with repeated questions.

Expected global answer-position balance is within one question across A/B/C/D.

## Lifecycle

Required candidate state:

```text
maturity:                     PERMANENT_ENGLISH_RUNTIME_CANDIDATE
reviewStatus:                 AWAITING_HUMAN_ENGLISH_REVIEW
englishImplementationFrozen: false
active:                       false
questionStudioDiscoverable:   false
questionBankStatus:           NOT_STORED
testEligibility:              INELIGIBLE
publiclyPublishable:           false
```

## Next gate

After machine proof and artifact export, the 59-question review must be audited as an exam setter, including:

- natural SSC/government-exam wording;
- plausible distractors rather than arithmetic noise;
- state-specific worked explanations;
- correct unit and rounding presentation;
- no irrelevant family trap leaking into a source;
- realistic source/target dimensions;
- adequate question-bank entropy;
- permanent-family difficulty and source breadth.

Only after any remediation should English freeze be considered. Question Studio activation remains a later independent gate.
