# TSD-CP-003

**Checkpoint:** Speed Changes, Schedules, Early-Late Arrival and Stops  
**Current phase:** `POST_OVERLAP_EXAM_READINESS_REVIEW`  
**Permanent QLs:** `0`  
**English:** `UNFROZEN`  
**Question Studio / Question Bank / tests / public delivery:** locked

## Discovery foundation

Executable discovery remains intentionally broad so source evidence is not lost:

- 35 authoritative source candidates;
- 24 provisional mathematical authorities;
- 22 learner-facing discovery modes;
- 2 internal QA modes;
- exact rational solver and independent verifier coverage;
- deterministic parameter generation, exam-style stems, method-derived distractors and six-step explanations;
- 40-seed runtime proof per learner discovery mode.

## Accepted post-overlap boundary

The ownership and first blind exam-readiness review now produce:

- **10** genuinely new CP-003 learner authority candidates;
- **2** discovery modes merged into another new CP-003 authority;
- **9** discovery modes retained as CP-003 representation extensions of finalized CP-001/002 authorities;
- those 9 representation families map to **8 distinct prior authority targets**;
- **1** discovery mode, `scheduleBuffer`, rejected as a standalone learner authority because it is only direct duration subtraction and does not add a distinct TSD skill;
- all 35 source candidates remain accounted for as accepted, represented, internal-QA or rejected discovery evidence;
- **0** permanent QLs allocated.

The accepted learner review is rebuilt from deterministic generation rather than copied from the discovery sample. It requires three distinct stems, three distinct mathematical states and three distinct answers for every accepted discovery family:

- **21 accepted discovery families × 3 = 63 learner rows**;
- **18 represented authority targets**: 10 new CP-003 targets + 8 existing CP-001/002 targets;
- the frozen CP-001/002 English inventory is not mutated.

## Blind-review remediations already implemented

- contradictory slower-speed wording corrected (`decreases`, not `increases`);
- same-arrival start-shift questions now explicitly ask **earlier** or **later** according to the generated state;
- combined arrival-shift questions explicitly ask for the **magnitude** because the answer contract is unsigned duration;
- weak repeated number pools expanded for recovery speed, changed-route percentage, lost-time reconstruction and start-time shift;
- awkward one-eighth-hour regular-stop profile removed;
- human/commuter-heavy contexts replaced by plausible route vehicles;
- same-arrival distractors now have a collision-safe, calculation-derived fallback rather than arbitrary nearby values.

## Next gate

After the current CI proof is green, the next work is:

1. blind-review the accepted 63-row corpus end to end for real SSC/banking/Punjab exam feel;
2. calibrate Easy/Medium/Hard labels from actual operations, not provisional tags;
3. remediate any remaining weak stems, contexts or distractor methods;
4. approve the nine representation-extension families without reopening the frozen CP-001/002 English corpus;
5. finalize merge/split ownership and only then allocate permanent QLs / freeze CP-003 English.
