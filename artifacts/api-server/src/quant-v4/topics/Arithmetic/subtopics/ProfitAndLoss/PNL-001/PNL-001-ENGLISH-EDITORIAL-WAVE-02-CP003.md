# PNL-001 English Editorial Wave 02 — CP-003

## Scope

Replace the single generic explanation tail used by all 24 `PNL-CP-003` dynamic QLs with solve-mode-specific, generated-value-bound working.

## Inventory

```text
QL range:                  PNL-QL-071..094
QLs:                       24
Underlying solve modes:    17
Proof seeds per QL:        24
Expected proof packages:   576
```

## Editorial contract

Every generated explanation must:

1. retain the frozen Editorial V2 concept, method and shortcut content;
2. add one `Generated-value check` paragraph;
3. cite the actual generated quantities, rates, cost/recovery values or target condition used by that package;
4. use inverse-specific wording for unknown rate, quantity, unit price and recovery tasks;
5. preserve statement, algebraic and data-sufficiency semantics;
6. end with the exact solver answer;
7. avoid the former CP-wide paragraph beginning `Working with these values`.

## Repetition gate

After numeric and currency normalisation, no generated-working paragraph may be shared by more than two QLs. A two-QL overlap is permitted only where the QLs deliberately share one mathematical solve mode but use different representations or answer semantics.

## Safety boundary

No stem, solver, option, Question Studio route, Question Bank write, test eligibility or publication metadata changes are included. All packages remain unreviewed dynamic candidates, not stored, test-ineligible and non-public.
