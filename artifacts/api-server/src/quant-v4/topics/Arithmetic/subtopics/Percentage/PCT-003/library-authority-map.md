# PCT-003 Library Authority Map

- `task-registry.library.json` owns CP ↔ QL ↔ task-kind ↔ answer-type mapping
- `question-language.*.json` owns stem language families
- `explanation.*.json` owns explanation ID mapping
- `variable-ranges.library.json` records first-pass variable authority
- `coverage-targets.library.json` records chapter coverage targets
- `distribution-targets.library.json` records expected distribution targets
