# PNC-CP-012 Implementation Report

## Checkpoint

- canonical problem: `PNC-CP-012 — Mixed Advanced Counting Systems`;
- permanent QLs: `PNC-QL-242` through `PNC-QL-269`;
- English QLs: 28;
- solve modes: 28;
- difficulty: 2 Easy / 12 Medium / 14 Hard;
- maturity: `RUNTIME_PROOF`;
- publication: disabled.

## Runtime architecture

The checkpoint contains:

- a human-owned English question-language inventory;
- one-to-one task-registry ownership;
- curated deterministic parameter pools;
- exact `bigint` production authorities;
- QL-specific explanations;
- misconception-driven four-option generation;
- independent verification appropriate to each family;
- structural, mathematical, editorial and TeX validation;
- JSON and CSV review exports.

## Independent verification

The runtime does not verify a formula by repeating the same formula. It uses:

- explicit category subsets and office loops;
- admissible-subset and permutation enumeration;
- reference-fixed circular and canonical dihedral representatives;
- complete permutation enumeration with fixed-position predicates;
- recursive monotone-path enumeration with visited checkpoints;
- complete weak-composition enumeration under bound vectors;
- live distinct-object assignment under capacities;
- paired colour-composition enumeration;
- canonical equal-team partition enumeration.

## Final proof snapshot

The exact runtime proof validates:

- 224 deterministic packages, each generated twice;
- 84 additional reviewed-TeX packages;
- 101 distinct rendered stems;
- all four correct-answer positions;
- direct production/independent-answer agreement;
- 28 unique QL IDs and 28 unique solve modes;
- exact 2/12/14 difficulty snapshot;
- zero missing IDs;
- zero duplicate templates;
- zero invalid audit samples;
- a 28-row reviewer export.

## Review-driven repairs

Validation exposed and corrected:

- a scalar/array evidence narrowing defect;
- invalid JSON escaping for authored MathJax delimiters;
- an irrelevant hidden `fixedCount` parameter on four fixed-point QLs;
- a neutral `committeeSize` workaround, replaced by proper optional handling;
- a redundant zero term in the two-colour inclusion-exclusion display;
- an unstated zero-binomial convention in non-uniform bounded stars and bars.

The proof was strengthened after review; checks were not relaxed.

## Safety boundary

- English only;
- no shared-composer registration;
- no Question Studio or Question Bank write path;
- no test/public routing;
- every generated package remains `publiclyPublishable: false`;
- manual approval and stacked merge-order reconciliation remain separate actions.
