# PNC-CP-011 — Final Implementation Report

## Verdict

`SATURATED FOR CURRENT ENGLISH OWNERSHIP AT RUNTIME-PROOF MATURITY`

The result is need-based. No QL count or solve-mode count was fixed before discovery, source review, ownership separation, duplicate audits and executable gap checks.

## Final inventory

- QL range: `PNC-QL-209` through `PNC-QL-241`;
- English QLs: 33;
- materially distinct solve modes: 30;
- difficulty: 3 Easy / 17 Medium / 13 Hard;
- next immutable PNC family ID: `PNC-QL-242`.

## Implemented waves

### Grouping Wave 1 — `PNC-QL-209..218`

- named prescribed groups;
- unnamed equal, unequal and repeated-size groups;
- unnamed pairs;
- specified-pair same/different restrictions in named and unnamed equal groups.

Ten QLs reuse seven authorities where the underlying solve contract is identical. The merges are intentional and prevent cosmetic mode inflation.

### Distinct-object Distribution Wave 1 — `PNC-QL-219..228`

- labelled receivers with unrestricted, onto, exact-use and empty-receiver conditions;
- exact occupancy vectors and specified-receiver occupancy;
- identical receivers through Stirling and Bell authorities.

### Identical-object Distribution Wave 2 — `PNC-QL-229..238`

- weak and positive compositions;
- exactly a stated number of labelled receivers used;
- empty-receiver complements;
- common and specified-receiver minimums;
- combined all-non-empty and specified-minimum conditions;
- controlled equal-capacity inclusion-exclusion;
- identical receivers through exact and bounded integer partitions.

The capacity mode owns one uniform capacity only. Non-uniform capacity vectors remain deferred mixed systems.

### Bounded inverse wave — `PNC-QL-239..241`

- recover total people from unnamed equal-group division counts;
- recover labelled receiver count for distinct-object assignments;
- recover labelled receiver count for identical-object weak compositions.

Every inverse exposes a bounded candidate domain and requires exactly one target match.

## Runtime architecture

The four runtime layers provide:

- human-owned English stems and QL-specific explanations;
- explicit task registries, constraint profiles and curated parameter pools;
- exact `bigint` production authorities;
- algebraically or enumeratively independent verification;
- misconception-driven four-option generation;
- deterministic seeded generation;
- solver-owned MathJax;
- control-character and TeX-command contracts;
- structural, editorial and coverage audits;
- JSON and CSV reviewer exports;
- unpublished runtime-proof packages.

## Independent authorities

The proof stack includes:

- sequential subset selection for prescribed groups;
- exhaustive labelled assignment enumeration;
- restricted-growth unlabelled set partitions;
- exhaustive weak-composition enumeration;
- nondecreasing integer-partition enumeration;
- bounded candidate-vector verification for inverse problems.

## Defects found and repaired during review

- answer-ceiling overflow in a grouping distractor pool;
- singular/plural constructions in grouping explanations;
- missing receiver-count aliases in identical-receiver states;
- JavaScript escaping that collapsed `\binom`, `\frac`, `\sum` and TeX spacing;
- capacity-formula braces being mistaken for content placeholders;
- a capacity state whose answer coincided with an unrelated all-non-empty method;
- unnatural identical-file wording in the capacity stem.

Each repair was followed by exact runtime and regression proof. Validation was strengthened rather than weakened.

## Saturation and boundaries

All 33 executable discovery candidates are admitted, merged into an existing authority where mathematically identical, or refined into a cleaner bounded inverse contract.

The following remain outside current CP-011 ownership and are deferred to CP-012 unless future evidence establishes a clean standalone authority:

- non-uniform capacity vectors;
- mixed distinct and identical object pools;
- per-object eligibility matrices;
- category quotas coupled across several groups;
- distribution followed by internal arrangement;
- large generating-function or other mixed casework systems.

## Release safety

- English only;
- shared PNC-002 composer integration deferred;
- no generation-engine registration;
- no Question Studio discovery;
- no Question Bank write path;
- no test or public routing;
- every generated package remains `publiclyPublishable: false`.

The PR remains a draft stacked on CP-010 until manual approval and merge-order reconciliation.
