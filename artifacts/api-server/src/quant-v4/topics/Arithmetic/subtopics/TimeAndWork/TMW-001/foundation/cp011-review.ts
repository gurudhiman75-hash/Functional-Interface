import { TMW_CP_011_REGISTRY } from "./cp011-registry";
import { runTmwCp011Pipeline } from "./cp011-runtime";
export function buildTmwCp011ReviewRows(perQl=3){return TMW_CP_011_REGISTRY.flatMap(entry=>{const rows=[];const seen=new Set<string>();for(let i=0;i<500&&rows.length<perQl;i++){const q=runTmwCp011Pipeline(entry.qlId,`review-${entry.qlId}-${i}`);if(!q.validation.valid)throw new Error(`${entry.qlId}: ${q.validation.errors.join(", ")}`);if(seen.has(q.mathematicalFingerprint))continue;seen.add(q.mathematicalFingerprint);rows.push({qlId:entry.qlId,solveMode:entry.solveMode,difficulty:entry.difficulty,stem:q.stem,options:q.options,correctIndex:q.correctIndex,correctAnswer:q.solution.answerText,keyRule:q.explanation.opening,formula:q.explanation.formula,givens:q.explanation.givens,standardSteps:q.explanation.steps,shortcutTitle:q.explanation.shortcut.title,shortcutSteps:q.explanation.shortcut.steps,commonTrap:q.explanation.commonTrap,conclusion:q.explanation.conclusion,validationStatus:"PASS",mathematicalFingerprint:q.mathematicalFingerprint});}if(rows.length!==perQl)throw new Error(`insufficient distinct states for ${entry.qlId}`);return rows;});}
