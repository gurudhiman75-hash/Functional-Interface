import { TMW_CP009_REGISTRY } from "./cp009-registry";
import { runTmwCp009Pipeline } from "./cp009-runtime";
export interface TmwCp009ReviewRow{qlId:string;solveMode:string;difficulty:string;seed:string;mathematicalFingerprint:string;stem:string;options:string[];correctIndex:number;correctAnswer:string;keyRule:string;formula:string;givens:string[];standardSteps:string[];shortcutTitle:string;shortcutSteps:string[];commonTrap:{optionLabel:string;optionText:string;misconceptionId:string;explanation:string};conclusion:string;validationStatus:string;}
export function buildTmwCp009ReviewRows(perQl=3):TmwCp009ReviewRow[]{const rows:TmwCp009ReviewRow[]=[];for(const entry of TMW_CP009_REGISTRY){const fingerprints=new Set<string>(),stems=new Set<string>();for(let attempt=0;attempt<400&&fingerprints.size<perQl;attempt+=1){const seed=`tmw-cp009-review:${entry.qlId}:${attempt}`,q=runTmwCp009Pipeline({questionLanguageId:entry.qlId,seed});if(!q.validation.valid)continue;if(fingerprints.has(q.mathematicalFingerprint)||stems.has(q.stem))continue;fingerprints.add(q.mathematicalFingerprint);stems.add(q.stem);rows.push({qlId:entry.qlId,solveMode:entry.solveMode,difficulty:entry.difficulty,seed,mathematicalFingerprint:q.mathematicalFingerprint,stem:q.stem,options:q.options,correctIndex:q.correctIndex,correctAnswer:q.solution.answerText,keyRule:q.explanation.opening,formula:q.explanation.formula,givens:q.explanation.givens,standardSteps:q.explanation.steps,shortcutTitle:q.explanation.shortcut.title,shortcutSteps:q.explanation.shortcut.steps,commonTrap:q.explanation.commonTrap,conclusion:q.explanation.conclusion,validationStatus:q.validation.valid?"PASS":"FAIL"});}if(fingerprints.size!==perQl)throw new Error(`${entry.qlId} could not provide ${perQl} distinct mathematical review states`);}return rows;}
