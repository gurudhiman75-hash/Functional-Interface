import { TMW_CP010_REGISTRY } from './cp010-registry';
import { runTmwCp010Pipeline } from './cp010-runtime';
export interface TmwCp010ReviewRow{qlId:string;solveMode:string;difficulty:string;stem:string;options:string[];correctIndex:number;correctAnswer:string;keyRule:string;formula:string;givens:string[];standardSteps:string[];shortcutTitle:string;shortcutSteps:string[];commonTrap:{optionLabel:string;optionText:string;misconceptionId:string;explanation:string};conclusion:string;validationStatus:string;mathematicalFingerprint:string;}
export function buildTmwCp010ReviewRows(perQl=3):TmwCp010ReviewRow[]{const rows:TmwCp010ReviewRow[]=[];for(const entry of TMW_CP010_REGISTRY){const seen=new Set<string>();for(let i=0;i<300&&seen.size<perQl;i++){const q=runTmwCp010Pipeline({questionLanguageId:entry.qlId,seed:`tmw-cp010-review:${entry.qlId}:${i}`});if(!q.validation.valid)throw new Error(`${entry.qlId}: ${q.validation.errors.join('; ')}`);if(seen.has(q.mathematicalFingerprint))continue;seen.add(q.mathematicalFingerprint);rows.push({qlId:entry.qlId,solveMode:entry.solveMode,difficulty:entry.difficulty,stem:q.stem,options:q.options,correctIndex:q.correctIndex,correctAnswer:q.solution.answerText,keyRule:q.explanation.opening,formula:q.explanation.formula,givens:q.explanation.givens,standardSteps:q.explanation.steps,shortcutTitle:q.explanation.shortcut.title,shortcutSteps:q.explanation.shortcut.steps,commonTrap:{...q.explanation.commonTrap},conclusion:q.explanation.conclusion,validationStatus:'PASS',mathematicalFingerprint:q.mathematicalFingerprint});}if(seen.size!==perQl)throw new Error(`${entry.qlId} lacks ${perQl} distinct mathematical states`);}return rows;}
