import { strict as assert } from "node:assert";
import { TMW_CP009_REGISTRY } from "./foundation/cp009-registry";
import { runTmwCp009Pipeline } from "./foundation/cp009-runtime";
import { verifyTmwCp009 } from "./foundation/cp009-engine";
import { buildTmwCp009ReviewRows } from "./foundation/cp009-review";
const correctPositions=new Set<number>(),stems=new Set<string>();let generatedCount=0;
for(const entry of TMW_CP009_REGISTRY){const qlStems=new Set<string>(),qlPositions=new Set<number>();for(let index=0;index<50;index+=1){const seed=`tmw-cp009-proof:${entry.qlId}:${index}`,first=runTmwCp009Pipeline({questionLanguageId:entry.qlId,seed}),second=runTmwCp009Pipeline({questionLanguageId:entry.qlId,seed});generatedCount+=1;assert.deepEqual(first,second);assert.equal(first.validation.valid,true,`${entry.qlId} ${seed}: ${first.validation.errors.join("; ")}`);assert.equal(verifyTmwCp009(entry,first.parameters,first.solution),true);assert.equal(first.options.length,4);assert.equal(new Set(first.options).size,4);assert.equal(first.options[first.correctIndex],first.solution.answerText);assert.equal(first.optionAudit.filter(option=>option.misconceptionId==="CORRECT").length,1);assert.equal(first.publiclyPublishable,false);assert.notEqual(first.explanation.commonTrap.optionText,first.solution.answerText);assert.ok(first.explanation.givens.length>=2);assert.ok(first.explanation.steps.length>=3);assert.ok(first.explanation.shortcut.title.startsWith("10-Second "));assert.ok(first.explanation.shortcut.steps.length>=2);correctPositions.add(first.correctIndex);qlPositions.add(first.correctIndex);stems.add(first.stem);qlStems.add(first.stem);}assert.equal(qlPositions.size,4,`${entry.qlId} did not reach all four answer positions`);assert.ok(qlStems.size>=3,`${entry.qlId} stem diversity is too low`);}
const rows=buildTmwCp009ReviewRows(3);assert.equal(rows.length,54);for(const entry of TMW_CP009_REGISTRY){const qlRows=rows.filter(row=>row.qlId===entry.qlId);assert.equal(qlRows.length,3);assert.equal(new Set(qlRows.map(row=>row.stem)).size,3,`${entry.qlId} review stems repeat`);assert.equal(new Set(qlRows.map(row=>row.mathematicalFingerprint)).size,3,`${entry.qlId} review mathematical states repeat`);}assert.equal(TMW_CP009_REGISTRY.length,18);assert.equal(generatedCount,900);assert.equal(correctPositions.size,4);assert.throws(()=>runTmwCp009Pipeline({questionLanguageId:"TMW-QL-157",seed:"locale",language:"hi"}));assert.throws(()=>runTmwCp009Pipeline({questionLanguageId:"TMW-QL-999",seed:"unknown"}));console.log(JSON.stringify({chapter:"TMW-001",checkpoint:"TMW-CP-009",qlCount:TMW_CP009_REGISTRY.length,seedsPerQl:50,generatedCount,distinctStems:stems.size,correctAnswerPositions:[...correctPositions].sort(),status:"PASS"},null,2));
