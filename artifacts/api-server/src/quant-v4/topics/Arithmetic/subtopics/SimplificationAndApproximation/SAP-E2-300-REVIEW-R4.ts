import assert from "node:assert/strict";
import fs from "node:fs";
import path from "node:path";
import { SAP_CP011_E2_STRUCTURES, generateSapCp011E2 } from "./SAP-002/SAP-CP-011/runtime-final";
import { SAP_CP012_E2_STRUCTURES, generateSapCp012E2 } from "./SAP-002/SAP-CP-012/runtime-release-r4b";
import type { SapE2Package } from "./SAP-E2-TYPES";

type Family={id:string;target:number;salt:number;generate:(seed:number)=>SapE2Package};
const t11=[20,12,12,20,3,3,5,5,2,4,2,2] as const;
const t12=[20,20,18,27,22,18,15,30,8,4,4,8,16] as const;
assert.equal(SAP_CP011_E2_STRUCTURES.length,12);assert.equal(SAP_CP012_E2_STRUCTURES.length,13);
const f11:Family[]=SAP_CP011_E2_STRUCTURES.map((id,i)=>({id,target:t11[i]!,salt:i+3,generate:s=>generateSapCp011E2(id,s)}));
const f12:Family[]=SAP_CP012_E2_STRUCTURES.map((id,i)=>({id,target:t12[i]!,salt:i+31,generate:s=>generateSapCp012E2(id,s)}));
assert.equal(f11.reduce((a,b)=>a+b.target,0),90);assert.equal(f12.reduce((a,b)=>a+b.target,0),210);
const rem=new Map([...f11,...f12].map(f=>[f.id,f.target]));const used=new Map<string,Set<number>>();let tick=0;
function next(pool:readonly Family[],avoid:string){const v=pool.filter(f=>(rem.get(f.id)??0)>0&&f.id!==avoid);const c=v.length?v:pool.filter(f=>(rem.get(f.id)??0)>0);if(!c.length)throw new Error("No family remaining");const mx=Math.max(...c.map(f=>rem.get(f.id)??0));const tied=c.filter(f=>(rem.get(f.id)??0)===mx).sort((a,b)=>((a.salt+tick)%97)-((b.salt+tick)%97));const f=tied[0]!;tick++;rem.set(f.id,(rem.get(f.id)??0)-1);return f;}
function seedFor(f:Family,idx:number){const set=used.get(f.id)??new Set<number>();used.set(f.id,set);const start=(set.size*7+f.salt*3)%25;for(let p=0;p<25;p++){const slot=(start+p)%25,seed=idx+1+4*slot;if(!set.has(seed)){set.add(seed);return seed;}}throw new Error(`${f.id}: exhausted answer bucket`);}
interface RR extends SapE2Package{questionId:string}const records:RR[]=[];
for(let i=0;i<300;i++){const is11=[2,5,8].includes(i%10),pool=is11?f11:f12,prev=records.at(-1)?.structureId??"";const f=next(pool,prev),idx=i%4,seed=seedFor(f,idx),q=f.generate(seed);assert.equal(q.correctIndex,idx);records.push({...q,questionId:`SAP-E2-${String(i+1).padStart(3,"0")}`});}
assert.equal(records.length,300);assert.equal(new Set(records.map(r=>r.stem)).size,300);assert.equal(new Set(records.map(r=>r.canonicalPayloadKey)).size,300);for(const f of [...f11,...f12])assert.equal(records.filter(r=>r.structureId===f.id).length,f.target,`${f.id}: quota`);
const pos=[0,0,0,0];let hard=0,bank=0,ssc=0,last="";for(const r of records){assert.equal(r.validation.ok,true,`${r.questionId}: ${r.validation.errors.join("; ")}`);assert.equal(r.options.length,4);assert.equal(new Set(r.options.map(o=>o.value)).size,4);assert.equal(r.options[r.correctIndex]?.value,r.canonicalAnswer);assert.ok(r.decisionCount>=2);assert.equal(r.lifecycle.permanentQlId,null);assert.equal(r.lifecycle.active,false);assert.equal(r.lifecycle.questionStudioDiscoverable,false);assert.equal(r.lifecycle.questionBankWritable,false);assert.equal(r.lifecycle.testEligible,false);assert.equal(r.lifecycle.publiclyPublishable,false);assert.doesNotMatch(r.stem,/For estimation, take|Using cancellation|using suitable approximation|oracle|runtime|prototype|canonical|machine policy|learner route|certified gap/i);assert.doesNotMatch(r.stem,/[√∛∜]/);assert.doesNotMatch(r.options.map(o=>o.value).join(" "),/Alternative\s+\d+/i);assert.notEqual(r.structureId,last);last=r.structureId;pos[r.correctIndex]!++;if(r.difficulty==="HARD")hard++;r.profile==="BANK"?bank++:ssc++;}
assert.deepEqual(pos,[75,75,75,75]);assert.ok(hard>=120);
const synthesis=records.filter(r=>r.structureId==="CP012-E2-MIXED-ROOT-POWER-SYNTHESIS");assert.equal(synthesis.length,16);assert.ok(synthesis.every(r=>r.difficulty==="HARD"&&r.decisionCount>=9));
const theory11=records.filter(r=>["CP011-E2-ABSOLUTE-ERROR","CP011-E2-PERCENTAGE-ERROR","CP011-E2-OVER-UNDER-DIRECTION","CP011-E2-COMPARE-ESTIMATE-ACCURACY","CP011-E2-COMPOSED-ROUNDING-BOUND","CP011-E2-OPTION-WITHIN-TOLERANCE","CP011-E2-GUARANTEED-NEAREST-FROM-INTERVAL","CP011-E2-AMBIGUOUS-OPTION-DIAGNOSIS"].includes(r.structureId)).length;const band12=records.filter(r=>["CP012-E2-UNIQUE-INTEGER-WITHIN-TOLERANCE","CP012-E2-COUNT-ADMISSIBLE-INTEGERS","CP012-E2-OUTCOME-CLASSIFICATION","CP012-E2-ROUNDED-OPERAND-SYNTHESIS"].includes(r.structureId)).length;
const summary={reviewVersion:"SAP-E2-CP011-CP012-PRODUCTION-R4B",questionCount:300,checkpoints:{cp011:90,cp012:210},profiles:{BANK:bank,SSC:ssc},structures:25,answerPositions:pos,hardQuestions:hard,cp011TheoryDiagnosticQuestions:theory11,cp012BandDiagnosticQuestions:band12,mixedRootPowerSynthesisQuestions:synthesis.length,lifecycle:"INACTIVE_HUMAN_REVIEW_CANDIDATE",permanentQlAllocation:"NONE"};
const out=path.join(process.cwd(),"artifacts/api-server/dist/quant-v4/sap-e2-review");fs.mkdirSync(out,{recursive:true});const md:string[]=["# SAP E2 — CP011 + CP012 — 300-Question Review R4B","",`Questions: **300**`,`Mix: **CP011 90 / CP012 210**`,`Profiles: **Bank ${bank} / SSC ${ssc}**`,`Structures: **25**`,`A/B/C/D: **${pos.join(" / ")}**`,`Mixed root-power synthesis: **${synthesis.length}**`,"","> Human review only. All E2 questions are inactive and unallocated.",""];for(const r of records){md.push(`## ${r.questionId} — ${r.profile} — ${r.difficulty}`,"",r.stem,"");r.options.forEach((o,i)=>md.push(`${String.fromCharCode(65+i)}. ${o.value}`));md.push("",`**Correct:** ${String.fromCharCode(65+r.correctIndex)} — ${r.canonicalAnswer}`,"",`**Idea:** ${r.explanation.coreConcept}`,"","**Working:**");r.explanation.steps.forEach((s,i)=>md.push(`${i+1}. ${s}`));md.push("",`**Final:** ${r.explanation.finalAnswer}`,"");}fs.writeFileSync(path.join(out,"SAP-E2-300-REVIEW.md"),md.join("\n"));fs.writeFileSync(path.join(out,"SAP-E2-300-REVIEW.json"),JSON.stringify({summary,records},null,2));fs.writeFileSync(path.join(out,"summary.json"),JSON.stringify(summary,null,2));
const esc=(s:string)=>String(s).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;");const cards=records.map(r=>`<section><h2>${r.questionId} — ${r.profile}</h2><p class="tag">${r.difficulty}</p><p class="stem">${esc(r.stem)}</p><ol type="A">${r.options.map(o=>`<li>${esc(o.value)}</li>`).join("")}</ol><div class="solution"><p><b>Correct:</b> ${String.fromCharCode(65+r.correctIndex)} — ${esc(r.canonicalAnswer)}</p><p><b>Idea:</b> ${esc(r.explanation.coreConcept)}</p><ol>${r.explanation.steps.map(s=>`<li>${esc(s)}</li>`).join("")}</ol><p><b>Final:</b> ${esc(r.explanation.finalAnswer)}</p></div></section>`).join("\n");fs.writeFileSync(path.join(out,"SAP-E2-300-REVIEW.html"),`<!doctype html><html><head><meta charset="utf-8"><title>SAP E2 Review R4B</title><script>MathJax={tex:{inlineMath:[['\\\\(','\\\\)']]},svg:{fontCache:'global'}};</script><script defer src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-svg.js"></script><style>body{font-family:Arial,sans-serif;max-width:1000px;margin:24px auto;padding:0 20px;line-height:1.55;background:#fafafa}header,section{background:#fff;border:1px solid #e2e2e2;border-radius:8px;padding:18px;margin:14px 0}h2{font-size:1rem}.tag{font-size:.8rem;font-weight:700;color:#555}.stem{font-size:1.05rem}.solution{border-top:1px dashed #ddd;margin-top:14px;padding-top:10px}li{margin:.25rem 0}</style></head><body><header><h1>SAP E2 — CP011 + CP012 Review R4B</h1><p>300 questions · CP011 90 · CP012 210 · 25 structures · Bank ${bank} · SSC ${ssc} · A/B/C/D ${pos.join(" / ")}</p></header>${cards}</body></html>`);console.log(JSON.stringify(summary));