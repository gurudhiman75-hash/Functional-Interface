# NUM-CP-003 — Checkpoint-Wide Provisional Merge, Split and Ownership Audit

**Status:** executable-discovery synthesis; source saturation still open  
**Temporary executable contracts reviewed:** 32  
**Permanent QLs:** 0  
**Frozen solve authorities:** 0

This audit prevents the 32 temporary prototypes from becoming a quota. It groups them by governing inference, answer semantic, uniqueness proof, misconception structure and ownership.

---

## 1. Provisional numerical solve authorities

### A. `APPLY_OR_RECOGNISE_DIVISIBILITY_RULE`

Current evidence:

- direct composite-divisor selection;
- divisor from stated rule;
- rule from stated divisor;
- positive/negative claim verification;
- concrete repeated-block divisibility where the full number is visible.

Provisional decision:

```text
one mathematical authority with parameters for:
- direction: number → divisibility result / rule → divisor / divisor → rule;
- output: divisor, rule or truth claim;
- primitive versus composite divisor;
- ordinary prose versus statement representation.
```

Claim verification is provisionally a representation adapter, not an independent numerical authority.

### B. `RESOLVE_SINGLE_MISSING_DIGIT`

Current evidence:

- unique missing digit;
- count of valid digits;
- complete valid-digit set;
- leading missing digit.

Provisional decision:

```text
one candidate-set engine with target semantics:
- unique digit;
- count;
- complete set;
- no/unique/multiple classification where later source-backed.
```

Leading position is an admissible-domain edge (`1..9` instead of `0..9`), not a separate numerical solver.

### C. `RESOLVE_DIGIT_IN_ARITHMETIC_RESULT`

Current evidence:

- hidden digit in addition result;
- hidden digit in difference;
- hidden digit in product.

Provisional decision:

```text
one exact arithmetic-result reconstruction authority
with operation as a generated parameter.
```

A later source review must decide whether division/quotient evidence belongs here or in `NUM-CP-007` under the division algorithm.

### D. `RESOLVE_ORDERED_MISSING_DIGIT_PAIRS`

Current evidence:

- unique ordered pair with an explicit digit-sum relation;
- unique ordered pair without that relation;
- count of all valid ordered pairs;
- complete ordered-pair set;
- no/unique/multiple solution classification.

Provisional decision:

```text
one ordered-pair solution-set engine with parameters for:
- optional auxiliary relation;
- one or more divisibility constraints;
- requested semantic: pair, count, set or solution class.
```

The answer semantic may still justify multiple learner-facing QLs after source and editorial review even if the mathematical engine is shared.

### E. `FIND_DIGIT_BOUND_MULTIPLE`

Current evidence:

- least n-digit multiple;
- greatest n-digit multiple.

Provisional decision:

```text
one boundary-optimisation authority with direction:
- first multiple at or above the lower boundary;
- last multiple at or below the upper boundary.
```

### F. `COUNT_RANGE_BY_DIVISIBILITY_PREDICATE`

Current evidence:

- multiples of one divisor;
- divisible by one divisor but not another;
- divisible by either of two;
- divisible by neither;
- divisible by exactly one;
- divisible by at least one of three divisors.

Provisional decision:

```text
one exact bounded-set counting framework with:
- divisor-set cardinality;
- target Boolean predicate;
- one-, two- or three-set inclusion–exclusion topology.
```

A later source/editorial audit must decide whether three-divisor inclusion–exclusion remains advanced enrichment rather than routine CP-003 content.

### G. `TEST_REPEATED_NUMERAL_DIVISIBILITY`

Current evidence:

- concrete source block repeated a stated number of times;
- direct exact divisibility of the resulting visible numeral.

Provisional decision:

```text
retain as a concrete concatenation/repeated-numeral representation candidate.
```

The general repeated-block identity below is not automatically the same learner task.

### H. `APPLY_ALGEBRAIC_DIVISIBILITY_IDENTITY`

Current evidence:

- guaranteed divisor of a power difference;
- guaranteed divisor of an odd power sum;
- guaranteed divisor from repeating a k-digit block twice.

Provisional decision:

```text
ownership hold — CP-003 / CP-008 / Algebra
```

These tasks use divisibility as the final answer but may be governed primarily by factor identities or modular arithmetic. They must not be frozen under CP-003 until source frequency and preferred exam method settle ownership.

---

## 2. Representation adapters, not automatic solve authorities

### Missing-digit data sufficiency

Executable evidence now covers all five standard outcomes across Waves 03–04:

- Statement I alone;
- Statement II alone;
- each statement alone;
- both together only;
- even together insufficient.

Provisional decision:

```text
representation adapter over the single-digit candidate-set engine.
```

### Divisibility claim verification

Provisional decision:

```text
statement/claim adapter over direct divisibility checking.
```

Future table and mini-caselet layouts remain representations unless they change the visible evidence or query contract.

---

## 3. Provisional reassignments

### Refer to `NUM-CP-008 — Modular Arithmetic and Simultaneous Congruences`

- least repunit length through the remainder recurrence;
- direct power-expression divisibility whose efficient engine is modular exponentiation.

These source fixtures must appear exactly once in the final chapter ledger.

### Refer to `NUM-CP-006 — HCF/LCM` where governing inference changes

- greatest divisor leaving the same remainder;
- hidden common-divisor reconstruction;
- least common divisibility led by LCM rather than digit evidence.

### Refer to `NUM-CP-007 — Division Algorithm` where governing inference changes

- hidden divisor, quotient or dividend from division-lemma evidence;
- quotient–divisor–remainder reconstruction.

### Refer to Algebra where governing inference changes

- general factorisation identities with no meaningful number-theory decision beyond symbolic manipulation.

---

## 4. Current compression result

```text
Temporary executable contracts reviewed:                 32
Provisional numerical authority families:                  8
Representation adapters identified:                        2
Explicit CP-008 reassignments:                              2 source families
Explicit CP-006/CP-007 boundary classes:                    held outside CP-003
Permanent QLs allocated:                                    0
Frozen solve authorities:                                   0
```

The provisional count of eight is not a final solve-mode count. Source saturation may merge, split, reject or add authorities.

---

## 5. Remaining gates before a count-bearing proposal

1. recover and classify all uploaded Number System source fixtures;
2. complete the Quant V2/V3 legacy fixture disposition ledger;
3. confirm rule-recognition and arithmetic-result source frequency;
4. decide whether answer semantics create separate learner-facing QLs over shared engines;
5. settle identity ownership across CP-003, CP-008 and Algebra;
6. settle repeated-numeral concrete versus identity ownership;
7. decide the advanced-enrichment boundary for three-divisor inclusion–exclusion;
8. run table/caselet representation gap checks;
9. perform an adversarial English review over the combined 96-question corpus;
10. produce a count-bearing QL-template proposal without permanent IDs.

No permanent `NUM-QL-*` allocation is authorised by this audit.
