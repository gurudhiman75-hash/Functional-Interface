# NUM-CP-003 — QL-Template Approval Checklist

The independent boundary review has already returned:

```text
APPROVE_WITH_MERGES_OR_SPLITS
22 first-pass rows → 17 revised rows
```

Before permanent allocation, review the consolidated 17-template runtime against these questions:

- Does each row still change the answer semantic, uniqueness predicate, evidence contract or misconception profile?
- Have largest/smallest and least/greatest remained parameters everywhere rather than reappearing as duplicate rows?
- Are the two rule-identification prototypes absent from learner QLs and retained only as study checks?
- Does the implicit repeated-numeral row always require construction rather than reveal the complete number?
- Does the linked arithmetic/divisibility row prove that divisibility materially reduces candidates?
- Do single-digit states support one or more divisibility constraints without multiplying QLs?
- Do all 23 retained prototype ancestries map to exactly one of the 17 rows?
- Do all 15 non-retained prototypes keep their reject, reassign, hold or study-only disposition?
- Are routine SSC patterns represented without advanced pair-set and data-sufficiency modes dominating generation?
- Do data sufficiency and claim verification use product-wide stable conventions?
- Are table and caselet layouts still adapters?
- Does every consolidated row pass exact generation, independent verification and human English review?

Final product-owner decision after consolidated runtime review must be one of:

```text
APPROVE_17_TEMPLATE_COUNT_FOR_PERMANENT_ALLOCATION
APPROVE_WITH_FINAL_MERGES_OR_SPLITS
RETURN_FOR_MORE_DISCOVERY
```

No decision automatically exposes Question Studio or publishes questions.
