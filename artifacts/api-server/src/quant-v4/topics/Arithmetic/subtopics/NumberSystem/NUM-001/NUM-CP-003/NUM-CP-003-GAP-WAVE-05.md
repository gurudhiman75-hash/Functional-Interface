# NUM-CP-003 — Gap Wave 05

**Status:** source-backed executable discovery candidate  
**Purpose:** close legacy answer-semantic gaps and replace decorative arithmetic-result divisibility  
**Temporary contracts:** 6  
**Permanent QLs:** 0  
**Frozen solve authorities:** 0

---

## 1. Why Wave 05 exists

Uploaded SSC and reference-book recovery plus Quant V3 disposition revealed five missing learner outputs over the existing single-digit candidate-set engine:

```text
largest valid digit
smallest valid digit
sum of valid digits
greatest completed number
smallest completed number
```

The same review rejected the current addition/difference/product prototypes as permanent CP-003 authorities because their visible equalities determine the digit before divisibility is used.

The SSC source also supplies a genuine replacement pattern in which arithmetic links two unknown digits and divisibility materially filters the arithmetic-compatible states before an extremum is selected.

---

## 2. Temporary contracts

```text
NUM-CP003-W5-PROT-LARGEST-VALID-DIGIT
NUM-CP003-W5-PROT-SMALLEST-VALID-DIGIT
NUM-CP003-W5-PROT-SUM-VALID-DIGITS
NUM-CP003-W5-PROT-GREATEST-COMPLETED-NUMBER
NUM-CP003-W5-PROT-SMALLEST-COMPLETED-NUMBER
NUM-CP003-W5-PROT-LINKED-ADDITION-DIVISIBILITY-EXTREMUM
```

The count of six is not a final QL count.

---

## 3. Shared candidate-set authority

The first five prototypes share one exact engine:

```text
admissible digit domain
  → complete substitution
  → exact divisibility check
  → valid digit set
  → target projection
```

Target projections are:

```text
maximum digit
minimum digit
sum of digits
completed numeral from maximum valid digit
completed numeral from minimum valid digit
```

Leading X uses domain `1..9`; every other position uses `0..9`.

---

## 4. Genuine linked arithmetic/divisibility contract

The linked prototype constructs states of the form:

```text
known addend + sAu = rBv
result divisible by k
find largest or smallest A
```

The generator proves:

1. the column relation leaves at least four arithmetic-compatible `(A, B)` pairs;
2. divisibility removes at least one arithmetic-compatible pair;
3. at least two pairs remain after divisibility, so the extremum request is material;
4. complete 100-pair enumeration independently reproduces the answer.

This explicitly prevents decorative divisibility.

---

## 5. Exactness and review architecture

- complete numerals use `bigint`;
- all single-digit domains are exhausted;
- linked two-digit states are independently checked over all 100 ordered pairs;
- every option has a state-derived diagnostic;
- each question includes concept, strategy, worked steps, shortcut, verification, conclusion and three traps;
- all lifecycle/publication surfaces remain disabled.

---

## 6. Validation plan

```text
Wave 01 regression
Wave 02 regression
Wave 03 regression
Wave 04 regression
Wave 05: 6 contracts × 100 seeds = 600 deterministic packages
Wave 05: 6 contracts × 60 seeds  = 360 structural/editorial packages
Wave 05 review export: 6 × 3     = 18 questions
```

The exact-head hosted review pack must be inspected before the checkpoint-wide retained-contract audit is updated.

---

## 7. Provisional disposition after proof

Expected retained effects:

- add five source-backed answer semantics over the shared candidate-set engine;
- add one linked hybrid if divisibility is demonstrably material;
- reject the earlier decorative addition/difference/product prototypes from the retained inventory;
- keep permanent allocation at zero until the post-Wave-05 merge/split and ownership audit closes.
