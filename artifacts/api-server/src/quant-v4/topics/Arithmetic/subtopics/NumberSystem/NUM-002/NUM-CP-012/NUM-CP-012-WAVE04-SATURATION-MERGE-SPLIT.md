# NUM-CP-012 — Wave 04 Final Source Saturation and Merge/Split

**Checkpoint:** `NUM-CP-012`  
**Package:** `NUM-002`  
**Status:** final discovery saturation candidate  
**Permanent allocation in this file:** none  
**Current next-free Number System identity:** `NUM-QL-226`

## Saturation result

Waves 01–03 exhaust the source-backed perfect-square, perfect-cube and general perfect-power demand retained by CP012 after ownership ablation. No unresolved source-backed gap requires another CP012 solve engine.

The discovery pool contains 14 temporary mathematical prototypes (`NUM-CP012-PROT-001..014`). Representation Wave 03 adds no new solve prototype. After merge/split ablation, these collapse to **11 retained permanent solve authorities**.

## Retained authority map

### Authority A — Perfect-power recognition and claim evaluation

Retains:
- `PROT-001` direct recognition;
- `PROT-013` terminal compatibility as rejection-only evidence;
- factorisation claim/statement evaluation representation.

Answer semantics may include integer selection, perfect-power class or boolean claim, but the governing solve engine is exact perfect-power classification.

### Authority B — Exact integer root and signed root-domain classification

Retains:
- `PROT-002` positive exact root;
- `PROT-009` zero/one, negative odd root and negative-even `NO_INTEGER_ROOT` states.

Canonical learner convention for non-negative even roots is the principal non-negative root. Signed exhaustive verification may discover `-r` as an equivalent witness but must not change the learner answer.

### Authority C — Least multiplier for perfect-power completion

Retains `PROT-003`. Add square/cube/general-`k` as parameters, including already-perfect multiplier `1`.

### Authority D — Least divisor for perfect-power reduction

Retains `PROT-004`, including already-perfect divisor `1`.

### Authority E — Missing exponent inverse family

Retains:
- `PROT-005` unique bounded missing exponent;
- `PROT-014` none/one/multiple solution topology;
- complete bounded candidate-set representation from the same enumerated state.

These share the same exponent-divisibility invariant and bounded candidate engine. Answer projection changes do not require separate permanent QLs.

### Authority F — Greatest perfect-power divisor

Retains `PROT-006`. Square/cube/general supported `k` values are parameters; requested answer is the divisor value, not its root.

### Authority G — Count perfect powers in a bounded interval

Retains `PROT-007` for square/cube interval counts. General `k` may be enabled only when exam-realistic and source-supported.

### Authority H — Additive boundary completion

Retains `PROT-008` with addition and subtraction as direction representations. Already-perfect completion `0` is valid unless wording explicitly requires a strictly next/previous power.

### Authority I — One-sided perfect-power bound value

Retains `PROT-010`: greatest perfect power not exceeding a bound / least perfect power at least a bound.

### Authority J — Nearest perfect-power value

Retains `PROT-011`. For ordinary integer-domain consecutive integer powers, equal-distance ties are impossible because consecutive powers have odd difference.

### Authority K — Least perfect-power multiple value

Retains `PROT-012`. It shares prime-exponent completion state with Authority C but returns the completed perfect-power multiple rather than the multiplier, so answer semantic and distractor topology justify the split.

## Explicit merges

- `PROT-001 + PROT-013 + factorisation claim` → Authority A.
- `PROT-002 + PROT-009` → Authority B.
- `PROT-005 + PROT-014 + full candidate-set projection` → Authority E.
- addition/subtraction directions inside `PROT-008` → Authority H.
- square/cube/general-`k` parameter variants merge within the same authority when learner reasoning is unchanged.

## Explicit splits

- recognition versus exact root → A vs B;
- least multiplier versus least divisor → C vs D;
- least multiplier versus least completed multiple value → C vs K;
- additive completion versus multiplicative completion → H vs C/D;
- bound value versus nearest value → I vs J;
- range count versus boundary value → G vs I/J.

## Reassignments and holds

- square/cube divisor count → `NUM-CP-005`;
- requested remainder/residue → `NUM-CP-008`;
- requested terminal digit(s) → `NUM-CP-009`;
- symbolic surd/exponent manipulation → Surds & Indices;
- polynomial square/cube identities → Algebra;
- area/volume context requiring geometry → Mensuration;
- Data Sufficiency → `DSF-001` reasoning/composition layer;
- independently essential multi-engine hybrid → `NUM-CP-014` candidate after ablation.

## Proposed permanent range

Final retained authority count: **11**.

Proposed only, not allocated by Wave 04:

```text
NUM-QL-226 .. NUM-QL-236
```

The next free identity after a successful permanent CP012 allocation would therefore become `NUM-QL-237`.

## Saturation closure

Wave 04 is satisfied because:

- recognition and exact roots cover positive, zero, one and signed-domain states;
- range count, one-sided bound, nearest and additive completion are separated by answer/algorithm semantics;
- multiplier, divisor, greatest perfect-power divisor and least perfect-power multiple are covered;
- missing exponent reaches unique, none, one, multiple and full-set projections;
- terminal compatibility is represented without CP009 authority leakage;
- claim evaluation is represented without QL inflation;
- Data Sufficiency is reassigned to its dedicated composition layer;
- cross-topic boundaries are explicit;
- no unresolved source-backed gap requires another CP012 solve engine.

No permanent identity is allocated in this document. Permanent allocation may proceed only after the Wave 04 executable saturation audit passes.
