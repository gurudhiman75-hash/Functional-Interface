export function formatCount(value: bigint): string { return value.toString(); }
export function formatCombination(n: number, r: number): string { return `${n}C${r}`; }
export function formatPermutation(n: number, r: number): string { return `${n}P${r}`; }
