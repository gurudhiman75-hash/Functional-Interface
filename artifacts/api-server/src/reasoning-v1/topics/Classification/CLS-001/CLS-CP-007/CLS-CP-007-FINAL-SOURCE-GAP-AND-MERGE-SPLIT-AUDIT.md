# CLS-CP-007 — Final Source-Gap and Merge/Split Audit

Status: `SOURCE_GAP_CLOSED__TWO_PERMANENT_CONTRACTS_AUTHORISED_FOR_RUNTIME_PROOF`

## Scope

This audit evaluates the complete CP-007 executable evidence after both source waves:

1. direct classification among complete three- to five-letter clusters;
2. direct classification among complete self-contained pairs of three-letter clusters.

It decides the permanent English learner-contract boundary. Runtime proof and artifact review must still pass before the checkpoint is finally frozen.

## Evidence inventory

### Complete single clusters

```text
Temporary task directions:             1
Temporary source prototypes:          13
Admitted bounded rules:               13
Logical audit questions:             390
Source-shaped quality questions:     156
Single-cluster reviewer questions:    52
Cluster lengths:                    3, 4, 5
Option counts:                         4, 5
Difficulty bands:        EASY, MEDIUM, HARD
```

### Complete cluster pairs

```text
Temporary task directions:             1
Source-backed prototypes:              1
Competing-rule universe:               8
Common opposite-transform domain:  15,600
Governed balanced near misses:     18,300
Executable audit questions:           240
Cluster-pair reviewer questions:       24
Option counts:                         4, 5
Difficulty bands:           MEDIUM, HARD
```

Every accepted question is independently reparsed from the displayed options and solved against its complete compatible registry.

## Single-cluster compression

All thirteen single-cluster rule families share the same final learner action and answer object:

```text
inspect each complete cluster
  -> recover the common internal structure
  -> identify the one complete cluster with a different structure
```

The following remain instance variables rather than separate QLs:

- cluster length three, four or five;
- exact signed or absolute gap values;
- direction of movement;
- exact vector, reduced ratio or equality topology;
- vowel count or repeated-letter pattern;
- complete position total;
- indexed equations and indexed pair relations;
- option count, answer position and difficulty.

The proof trace changes by rule, but the learner contract, answer object, mismatch semantics and independent verification topology remain one complete cluster.

Decision:

```text
Permanent QL: CLS-QL-012
Solve contract: CP007-FIND-ODD-LETTER-CLUSTER
```

## Cluster-pair compression

The recovered source inventory contains one repeated concrete self-contained cluster-pair authority: position-wise opposite-letter transformation. No second concrete source-backed pair transformation was recovered.

Generic signed shifts, absolute shifts, direct reversal and reversed-opposite relations remain in the competing-rule registry so accidental alternative answers are rejected. They do not become source prototypes or separate QLs without repeated source authority.

All accepted opposite-transform pair questions share this learner action:

```text
inspect the position-wise relation inside every complete cluster pair
  -> identify the relation shared by all but one pair
  -> select the complete cluster pair with the different relation
```

Corresponding position-sum vector and named opposite-transform status are mathematically equivalent for the intended value `27,27,27`. They may support the same answer and do not create separate QLs.

Decision:

```text
Permanent QL: CLS-QL-013
Solve contract: CP007-FIND-ODD-LETTER-CLUSTER-PAIR
```

## Why CLS-QL-012 and CLS-QL-013 do not merge

The final response in both tasks is an odd option, but the displayed answer objects and proof topologies differ materially.

### CLS-QL-012

- answer object: one complete cluster;
- evidence: relations among letters inside that cluster;
- parser: one token sequence;
- verifier: one cluster-rule registry.

### CLS-QL-013

- answer object: one complete pair of clusters;
- evidence: a position-wise transformation from the left cluster to the right cluster inside the same option;
- parser: two token sequences plus their pairing boundary;
- verifier: a cross-cluster transformation registry.

Merging them would hide a real change in answer semantics and independent proof structure.

## Ownership confirmation

The cluster-pair contract remains Classification because every answer option is complete and self-contained. It does not supply one external source pair whose relation must be transferred to an incomplete target.

The following remain outside CP-007:

- source-to-incomplete-target transfer — Analogy;
- next, missing or wrong cluster in a progression — Series;
- hidden encode/decode transformation — Coding-Decoding;
- direct requested position, count or rearranged output — Alphabet Test;
- single letters and complete two-letter pair answers — CP-006;
- mixed letter-number or symbol clusters — CP-008.

## Source-gap decision

```text
Meaningful uncovered single-cluster contracts: 0
Meaningful uncovered cluster-pair contracts:   0
Additional source-backed pair transforms:       0
Permanent learner contracts authorised:         2
Permanent QLs authorised:                       2
Next unreserved QL after this checkpoint:       none
```

No later `CLS-QL-*` identity is reserved by this audit.

## Permanent runtime requirements

Before final freeze, the permanent English gate must prove:

- deterministic replay for both QLs;
- all thirteen single-cluster rules and prototypes;
- all three single-cluster lengths;
- the complete eight-rule cluster-pair verifier;
- four- and five-option forms;
- every answer position;
- all applicable difficulty bands;
- unique answers under complete competing-rule enumeration;
- source-shaped close outliers and matched nuisance features;
- balanced opposite-pair near misses preserving the whole total;
- one explicit match/fail conclusion per option;
- no internal IDs in learner text;
- permanent lifecycle and publication locks.

## Lifecycle boundary pending proof

```text
permanentQlIds:             CLS-QL-012, CLS-QL-013
reviewStatus:               PROVISIONAL_PERMANENT_ENGLISH
questionBankStatus:         NOT_STORED
testEligibility:            INELIGIBLE
publiclyPublishable:        false
questionStudioDiscoverable: false
```
