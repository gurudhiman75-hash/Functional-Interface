# CLS-CP-008 — Final Mixed-Token Ownership Closure

Status: `CLOSED_ZERO_NEW_QL__OWNERSHIP_BOUNDARY_FROZEN`

## Final decision

`CLS-CP-008` creates no new permanent Question Language and no question generator.

The checkpoint was deliberately reserved until CP-001 through CP-007 stabilised so that mixed-token material could be tested against mature chapter boundaries. The re-audit found no genuinely new self-contained Classification solver contract.

## Proven dispositions

```text
Candidate families audited:             11
Concrete source controls audited:         6
Merge into existing Classification QL:    1
Reassign to dedicated chapters:           7
Reject for source gap:                     3
New permanent QLs:                         0
New runtime generators:                    0
```

### Existing-Ql merge

The text-safe punctuation-versus-operator form merges into:

```text
CLS-QL-001 — Find the semantic outlier among four or five items
```

The visible glyph representation does not change the semantic membership task. This closure does not modify the existing frozen CP-001 dataset or runtime.

### Reassigned owners

- visual symbol and letter-number layouts → `REAS-FCL` Figure Classification;
- hidden letter/number/symbol mappings → `REAS-COD` Coding-Decoding;
- mixed-sign equations and operator replacement → `REAS-OPS` Mathematical Operations;
- ordered mixed-token progressions → `REAS-SER` Series;
- row-column mixed-token synthesis → `REAS-MAT` Matrix.

### Rejected proposals

- free-form letter-number odd groups;
- arbitrary combined semantic/structural multi-condition states;
- Classification caselets or tables without recurring source authority.

These remain rejected until a later source corpus proves a bounded, recurring and materially distinct Classification contract.

## Chapter consequence

The permanent English Classification inventory remains:

```text
CLS-QL-001 through CLS-QL-013
```

No `CLS-QL-014` identity is reserved.

## Lifecycle

```text
permanentQlCount:             0
questionStudioDiscoverable:   false
questionBankWritable:         false
testEligible:                 false
publiclyPublishable:          false
```

## Reopening rule

CP-008 may be reopened only when all of the following are supplied:

1. recurring source evidence from the target SSC, Banking or Punjab exam corpus;
2. a complete self-contained answer object;
3. a bounded compatible-rule universe;
4. proof that the task is not already owned by CLS-QL-001 through CLS-QL-013 or another Reasoning chapter;
5. executable ambiguity, editorial and lifecycle validation.

Mixed token type alone is never sufficient.
