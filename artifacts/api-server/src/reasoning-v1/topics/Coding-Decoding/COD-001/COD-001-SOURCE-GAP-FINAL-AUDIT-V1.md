# COD-001 — Source-Gap Final Audit V1

Status: **CLOSED FOR ENGLISH IDENTITY — FOUR PERMANENT QLs ALLOCATED; LOCALIZATION AND INTEGRATION REMAIN SEPARATE**

Date: 2026-09-13

Current allocation authority: `COD_SOURCE_GAP_DISCOVERY_FREEZE_V1` + `COD-001-MANIFEST-AMENDMENT-SOURCE-GAP-V1.md`

This final-audit wave began from the earlier guarded source-gap prototype and has now completed source proof, merge/split, ownership, production-quality remediation, versioned discovery freeze and permanent English identity allocation.

The pre-existing `COD-QL-001..199` identities remain unchanged. The approved source-gap identities are now `COD-QL-200..203`. All four remain review-only and are not exposed to Question Studio, Question Bank, mock tests or public publication.

## 1. Final permanent allocation

| QL ID | Owner | Solve contract | Rule authority |
|---|---|---|---|
| `COD-QL-200` | `COD-CP-005` | infer alphabetical ascending sort and encode target | `ALPHABETICAL_ASCENDING_SORT` |
| `COD-QL-201` | `COD-CP-006` | infer indexed shifts then reverse and encode target | `INDEXED_SHIFT_THEN_REVERSE` |
| `COD-QL-202` | `COD-CP-006` | infer reverse then uniform shift and encode target | `REVERSE_THEN_UNIFORM_SHIFT` |
| `COD-QL-203` | `COD-CP-007` | infer mixed vowel/consonant class coding and encode target | `MIXED_CLASS_CODE` |

`COD-QL-204` is not reserved or automatically available.

## 2. Merge / split decision

### `ALPHABETICAL_ASCENDING_SORT`
Retained as one CP005 solve contract. It is value-dependent sorting, not a fixed position permutation, so existing CP005 rearrangement QLs cannot absorb it.

### `INDEXED_SHIFT_THEN_REVERSE`
Retained as one CP006 solve contract. It is materially distinct from existing reverse-then-indexed-shift because the two stages do not commute.

### `REVERSE_THEN_UNIFORM_SHIFT`
Retained as one CP006 solve contract. Shift direction and magnitude remain generated-instance parameters, not separate QLs.

### `MIXED_CLASS_CODE`
Retained as one CP007 solve contract. The two source-backed mapping variants remain contexts of this one solve architecture and are not separate QLs.

## 3. Task-direction freeze

The approved task direction for all four identities is only:

```text
infer hidden rule from two displayed word→code examples, then encode one target word
```

No identities are allocated for:

- inverse decoding;
- missing-token recovery;
- choose-matching-code as a separate QL;
- explicit-rule forward application;
- rule naming/classification;
- either mixed-class mapping context as a separate QL.

Future expansion requires fresh recurring source evidence and a new discovery cycle.

## 4. Production-quality remediation completed

The earlier HOLD blockers are closed.

### Instance-derived difficulty
Difficulty is now scored from generated state: target/evidence length burden, repeated letters, wraparound, composition burden, indexed base, uniform shift magnitude and mixed-class switching. Each permanent QL reaches multiple legitimate difficulty bands.

### Governed vocabulary and fatigue
The 32-word prototype pool was replaced by a governed 180+ word exam-neutral pool. Rolling target/evidence-pair fatigue gates are executable. Mixed-class instances visibly exercise both vowel and consonant channels.

### Beginner-first explanations
Mandatory shortcut/trap boilerplate was removed. Explanations preserve direct rule, source evidence, actual transformation steps and compact working. A caution appears only when it helps reject a concrete misconception.

### Distractor provenance
All permanent-runtime distractors come from explicit misconception models. Arbitrary fallback mutation is disabled and executable gates assert that it is never used.

## 5. Executable proof

All of the following are green on the allocation branch:

- original source-gap prototype: **480 questions**;
- V2 quality/fatigue matrix: **960 questions**;
- permanent discovery-freeze matrix: **960 questions** (240 per new QL);
- English source-gap extension closure: **480 questions** (120 per new QL);
- legacy exact-surface cross-collision sample: **1,592 questions**;
- source-gap exact collisions: **0**;
- sampled legacy cross-collisions: **0**;
- all four answer positions reached within every new QL;
- each new QL reaches multiple difficulty bands;
- exact audited source fixtures still reproduce;
- displayed evidence independently identifies exactly one rule/context.

The unchanged legacy `COD-QL-001..199` English closure remains green as a separate regression gate, so English permanent identity is now `COD-QL-001..203` without rewriting the mature 199-QL proof.

## 6. Ownership / collision result

The earlier cross-chapter collision review remains cleared:

- Alphabet Test / Word Order may use explicit sorting operations, but the source-gap CP005 task requires hidden-rule inference from word→code examples and therefore remains Coding-Decoding.
- CP006 stage-order contracts are distinct from existing compositions.
- CP007 mixed-class coding is not a duplicate of uniform modular digit translation.

No existing QL identity was repurposed.

## 7. Language and release boundary

Current language frontier:

```text
English: COD-QL-001..203 permanent runtime-proof
Hindi:   COD-QL-001..199 localized runtime-proof
Punjabi: COD-QL-001..199 localized runtime-proof
```

`COD-QL-200..203` remain:

- review-only;
- Question Studio disabled;
- Question Bank writing disabled;
- mock-test eligibility disabled;
- public publication disabled.

Localization of the four new identities is the next separate content checkpoint. Guarded integration can occur only after the relevant language/editorial gates are complete.

## 8. Final audit verdict

The source-gap audit is **closed for English identity allocation**. The four late source-backed contracts are now permanent as `COD-QL-200..203`, with no task-template multiplication and no reopening of the revoked 260-QL legacy plan.
