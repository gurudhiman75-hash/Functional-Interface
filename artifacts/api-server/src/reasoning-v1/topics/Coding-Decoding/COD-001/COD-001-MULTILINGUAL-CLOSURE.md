# COD-001 — Multilingual Runtime Closure

Status: **closed at multilingual runtime-proof maturity; review-only**.

## Frozen chapter identity

```text
Checkpoints: COD-CP-001..010
Permanent QLs: COD-QL-001..199
Total permanent QLs: 199
Languages: en-IN, hi-IN, pa-IN
```

The legacy predetermined total of 260 QLs remains revoked. `COD-QL-199` is the evidence-backed end of the chapter. A future identity requires new recurring source evidence, ownership and collision audits, executable prototype proof, a new discovery freeze and an explicit manifest amendment.

## Locale architecture

- `COD-CP-001..007` and `COD-CP-010` use the frozen English solver with native Hindi/Punjabi instructions, rule descriptions, condition text and explanations.
- `COD-CP-008` uses complete language-adapted referent dictionaries and separately authored semantic facts.
- `COD-CP-009` uses complete injective Hindi/Punjabi lexicons and grammar-aware sentence rendering while preserving artificial code tokens and abstract constraint topology.

Every localized question preserves permanent QL ownership, seed determinism, correct option position, difficulty, renderer and the underlying solver authority.

## Final executable proof

The whole-chapter closure gate generates:

```text
199 QLs × 6 seeds × 3 locales = 3,582 questions
1,194 questions per locale
```

It proves:

- continuous and unique identity from `COD-QL-001` through `COD-QL-199`;
- all ten checkpoints in every locale;
- deterministic generation;
- four semantically unique options and exactly one marked answer;
- correct-index parity across English, Hindi and Punjabi;
- Hindi and Gurmukhi script presence;
- no English instructional fallback after protecting logic-neutral source data and code tokens;
- natural Punjabi terminology, including rejection of standalone `ਪਦ` and `ਸਾਦ੍ਰਿਸ਼ਤਾ`;
- all three difficulties and all five approved renderers in every locale;
- zero exact displayed-question collisions in English, Hindi and Punjabi;
- review-only release safety.

Answer-position counts are identical in all three locales:

```text
319 / 283 / 289 / 303
```

## Final manual review corpus

The closure workflow exports one deterministic sample for every QL in every language:

```text
199 English + 199 Hindi + 199 Punjabi = 597 review questions
```

Both JSONL and Markdown files are produced for editorial review.

## Release boundary

This closure does not enable product exposure:

```text
Question Studio: disabled
Question Bank conversion: disabled
Mock-test eligibility: disabled
Public routing/publication: disabled
```

A later, explicitly approved integration phase may consume this reviewed runtime without changing QL identity or solver authority.
