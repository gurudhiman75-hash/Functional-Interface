# IOP-001 — Checkpoint C Source Saturation V1

Status: **GAP WAVE REQUIRED — permanent QL allocation still blocked**

Date: 2026-08-17

## Purpose

This pass separates **exam-backed reasoning contracts** from temporary executable probes. The 30 CP001–CP010 prototypes remain discovery authorities until this ledger closes.

## Evidence tiers

- **T1 — PYQ reconstruction:** named exam/date/shift and a complete machine illustration reproduced by a reputable prep source.
- **T2 — recent exam analysis:** current topic occurrence/weightage, but not enough to define exact machine semantics.
- **T3 — institutional / established theory:** stable taxonomy and worked examples suitable for family-level support.
- **T4 — coaching practice reconstruction:** useful corroboration, never sufficient alone for exotic permanent rules.

## Normalized evidence

### S1 — SATHEE Bank Exam Input–Output theory (T3)

Supports:

- one-sided alphabetical word placement;
- one-sided ascending number placement;
- alternating word-number placement;
- simultaneous smallest-left / largest-right placement;
- word-length ordering;
- reverse alphabetical ordering.

Disposition: strong family-level support for CP001, CP003, CP004 and part of CP005.

### S2 — Careers360 current Input–Output taxonomy (T3)

Supports the chapter-level taxonomy:

- single shifting;
- double shifting;
- operation-based input-output;
- box-based input-output.

Its worked operation example includes a multi-stage numeric pipeline involving digit reversal, ordering, digit-level transformation, digit sum and re-ordering.

Disposition: confirms CP006 and CP009 as real ownership families; exact operation chains still require individual evidence.

### S3 — Testbook / BankersAdda mixed rearrangement sets (T4)

Repeated worked sets show mixed word-number machines where categories are arranged by separate rules and intermediate-step questions are asked over the resulting trace.

Disposition: supports CP002 and mixed CP003/CP004 semantics.

### S4 — BankersAdda machine sets with attribute keys (T4)

Worked examples explicitly use:

- number digit sum as the ordering key;
- word last-letter alphabetical position as the ordering key;
- prime/composite grouping followed by numeric ordering.

Disposition: CP005 is broader than the current three prototypes. `DIGIT_SUM` is directly supported. `WORD_LENGTH` is supported by S1. `VOWEL_COUNT` remains a discovery-only probe. Last-letter and category-grouped number keys are source-backed gap candidates.

### S5 — RBI Grade B Phase 1, 8 Sep 2024 Shift 1 PYQ reconstruction (T1)

A complete mixed word-number machine is reproduced with these learner-visible semantics:

- words are selected alphabetically;
- selected words are transformed character-wise;
- numbers are selected by numeric order;
- selected numbers are transformed to derived values (digit-sum behavior is visible in the reproduced trace);
- a transformed word and transformed number are fixed together in each visible step.

Disposition: **mandatory CP008 gap**. The current homogeneous all-word/all-number advanced engine does not yet represent this reasoning contract.

### S6 — RBI Grade B Phase 1, 8 Sep 2024 Shift 2 and 28 May 2022 PYQ reconstructions (T1)

These sets show high-level mixed word-number machines with character transformations, digit transformations, ordering, symbolic replacement and later-stage synthesis.

Disposition: confirms CP007/CP008 advanced transformation ownership. Exact transforms must be source-normalized rather than freely synthesized.

### S7 — AffairsCloud / Careers360 box-operation worked sets (T3/T4)

Box-based machines perform arithmetic across cells/boxes over multiple stages.

Disposition: CP009 remains a permanent chapter family. The current `PAIR_REWRITE(sum, abs-diff)` probes are **not** promoted merely because they execute; production operations must be tied to a normalized worked pattern.

### S8 — recent Banking exam analyses, 2025 (T2)

SBI PO Mains and SBI Clerk Mains analyses continue to report Machine Input–Output sets, including word-based and word+number forms, commonly as 4–8 question sets depending on the paper.

Disposition: Banking Mains is the primary exam profile. The chapter remains low/conditional weight for SSC/Punjab unless an exam-specific source supports it.

## Prototype disposition

### CP001 — Basic one-sided rearrangement

- `CP001-PROT-001` word alphabetical ascending: **RETAIN**
- `CP001-PROT-002` reverse alphabetical/right-end variant: **MERGE as parameter**
- `CP001-PROT-003` number ascending: **RETAIN**

Expected permanent contracts: 2.

### CP002 — Mixed blocked rearrangement

All 3 prototypes: **MERGE** into one mixed blocked-category contract with category order, direction and placement as controlled parameters.

Expected permanent contracts: 1.

### CP003 — double-ended / simultaneous

- pure-number min/max simultaneous: **RETAIN**
- mixed word-number simultaneous variants: **MERGE**

Expected permanent contracts: 2.

### CP004 — alternating / interleaved

- mixed word-number alternation: **RETAIN/MERGE**
- pure-number min/max alternation: **QUARANTINE** unless a direct source example is normalized.

Expected permanent contracts: 1.

### CP005 — attribute-based selection

- word length: **RETAIN**
- digit sum: **RETAIN**
- vowel count: **QUARANTINE** from permanent allocation in V1

Gap candidates: last-letter alphabetical and grouped-number attributes.

Expected permanent contracts in V1: 2.

### CP006 — numeric operation machines

Family: **RETAIN**.

The three exact synthetic pipelines are engine probes. They **MERGE** into one numeric-operation pipeline authority after source-normalized operation whitelisting.

Expected permanent contracts: 1.

### CP007 — word/alphanumeric transformation

- word transformation pipeline: **RETAIN**
- alphanumeric transformation pipeline: **RETAIN as advanced capability**, but exact production transform whitelist remains source-gated.

Expected permanent contracts: 2.

### CP008 — multi-stage transformation

Current homogeneous prototypes: **ENGINE CAPABILITY ONLY**.

Required gap: mixed word+number transformed-pair machine based on RBI Grade B PYQ reconstruction.

Expected permanent contracts after gap: 1 mixed advanced contract; homogeneous modes become parameters/representations where source-backed.

### CP009 — box/table/cell

Topology family: **RETAIN**.

Current exact pair sum/difference pipelines: **QUARANTINE as synthetic probes** pending direct operation match.

Expected permanent contracts: 1 box-operation contract.

### CP010 — reverse / missing-state / synthesis

**DO NOT allocate machine QLs.**

`PREVIOUS_STEP`, `MISSING_STEP`, `STEP_NUMBER`, `REMAINING_STEP_COUNT` are query/solve modes over other machine authorities unless reverse execution introduces a materially new machine rule.

Expected permanent machine contracts: 0.

## Provisional merge result

Before gap closure, the source-backed contract count is approximately:

```text
CP001  2
CP002  1
CP003  2
CP004  1
CP005  2
CP006  1
CP007  2
CP008  1  <- blocked on mixed-transform gap
CP009  1
CP010  0  <- solve modes, not machine QLs
----------------
TOTAL  13 provisional machine contracts
```

This is **not yet permanent allocation**.

## Required gap wave before permanent IDs

1. Add an executable mixed word+number transformation authority based on a normalized RBI Grade B 2024 PYQ family.
2. Add at least one source-backed attribute-key gap beyond current prototypes (prefer last-letter alphabetical rather than vowel count).
3. Replace/quarantine source-unmatched CP009 arithmetic probes; retain topology engine but do not promote their exact math.
4. Re-run ambiguity, oracle, deterministic and scale proofs.
5. Perform merge/split audit again after the new gap authorities exist.

## Gate

```text
sourceSaturated:              false
permanentQlAllocationAllowed: false
permanentQlCount:             0
questionStudioDiscoverable:   false
questionBankWritable:         false
testEligible:                 false
publiclyPublishable:          false
```
