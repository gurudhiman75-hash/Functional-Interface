import { createHash } from "node:crypto";
import {
  DS_STANDARD_5_EN,
  SUFFICIENCY_CLASSES,
  evaluateFiniteDomainPair,
  optionForClass,
  type SufficiencyClass,
  type TwoStatementSufficiencyEvaluation,
} from "../foundation/index.ts";
import { solveMen001 } from "../../../../../quant-v4/topics/AdvancedMathematics/subtopics/Mensuration/MEN-001/solver.ts";
import type { Men001Parameters, Men001SolveMode } from "../../../../../quant-v4/topics/AdvancedMathematics/subtopics/Mensuration/MEN-001/types.ts";
import { buildMenCp010State, solveMenCp010 } from "../../../../../quant-v4/topics/AdvancedMathematics/subtopics/Mensuration/MEN-002/cp010-foundation/engine.ts";
import type { MenCp010State } from "../../../../../quant-v4/topics/AdvancedMathematics/subtopics/Mensuration/MEN-002/cp010-foundation/types.ts";
import { exactKey, formatWithUnit } from "../../../../../quant-v4/topics/AdvancedMathematics/subtopics/Mensuration/MEN-002/foundation/exact.ts";

export const DSF_CP011_MENSURATION_RUNTIME_VERSION = "DSF_CP011_MENSURATION_RUNTIME_V1" as const;
export const DSF_CP011_MENSURATION_SOLVE_MODES = [
  "DSF-SM-MEN-TRIANGLE-AREA",
  "DSF-SM-MEN-RECTANGLE-AREA",
  "DSF-SM-MEN-RECTANGLE-PERIMETER",
  "DSF-SM-MEN-CIRCLE-AREA",
  "DSF-SM-MEN-CIRCLE-CIRCUMFERENCE",
  "DSF-SM-MEN-SQUARE-PYRAMID-VOLUME",
  "DSF-SM-MEN-CONICAL-FRUSTUM-VOLUME",
] as const;
export type DsfCp011MensurationSolveMode = (typeof DSF_CP011_MENSURATION_SOLVE_MODES)[number];
export type DsfCp011MensurationDifficulty = "Easy" | "Medium" | "Hard";
export type DsfCp011MensurationTargetKind = "AREA" | "PERIMETER" | "CIRCUMFERENCE" | "VOLUME";

type ContextId =
  | "TRIANGULAR_PLOT" | "TRIANGULAR_BOARD" | "RECTANGULAR_FIELD" | "RECTANGULAR_FLOOR"
  | "CIRCULAR_GARDEN" | "CIRCULAR_TRACK" | "PYRAMID_MODEL" | "PYRAMID_STRUCTURE"
  | "FRUSTUM_CONTAINER" | "FRUSTUM_MODEL";
type StatementFamily =
  | "BASE_EXACT" | "HEIGHT_EXACT" | "AREA_EXACT" | "BASE_HEIGHT_PAIR" | "DIMENSION_BOUND"
  | "LENGTH_EXACT" | "BREADTH_EXACT" | "PERIMETER_EXACT" | "LENGTH_BREADTH_PAIR"
  | "RADIUS_EXACT" | "DIAMETER_EXACT" | "CIRCUMFERENCE_EXACT" | "RADIUS_WINDOW_LOWER" | "RADIUS_WINDOW_UPPER" | "RADIUS_PARITY"
  | "PYRAMID_SIDE_EXACT" | "PYRAMID_HEIGHT_EXACT" | "PYRAMID_VOLUME_EXACT" | "PYRAMID_SIDE_HEIGHT_PAIR"
  | "OUTER_RADIUS_EXACT" | "INNER_RADIUS_EXACT" | "FRUSTUM_RADII_PAIR" | "FRUSTUM_HEIGHT_EXACT" | "FRUSTUM_VOLUME_EXACT" | "FRUSTUM_FULL_DATA"
  | "SOLID_BOUND";

interface TwoDWorld { readonly kind:"TRIANGLE"|"RECTANGLE"|"CIRCLE"; readonly a:number; readonly b?:number; readonly area?:string; readonly perimeter?:string; readonly circumference?:string; }
interface SolidWorld { readonly kind:"SQUARE_PYRAMID"|"CONICAL_FRUSTUM"; readonly state:MenCp010State; readonly answerKey:string; readonly answerDisplay:string; }
type MensurationWorld = TwoDWorld | SolidWorld;
interface MensurationProblem { readonly solveMode:DsfCp011MensurationSolveMode; readonly targetKind:DsfCp011MensurationTargetKind; readonly anchor:MensurationWorld; readonly contextId:ContextId; readonly intro:string; }
interface MensurationStatement { readonly id:string; readonly family:StatementFamily; readonly complexity:1|2|3; readonly text:string; readonly test:(world:MensurationWorld)=>boolean; }
interface SynthesizedPair { readonly statementI:MensurationStatement; readonly statementII:MensurationStatement; readonly evaluation:TwoStatementSufficiencyEvaluation<string>; readonly qualityScore:number; }

const TRIANGLE_CONTEXTS=[
 {id:"TRIANGULAR_PLOT" as const,intros:["A triangular plot has a fixed base and perpendicular height.","Consider a triangular plot measured by its base and perpendicular height.","The dimensions of a triangular plot are being examined.","A triangular piece of land is measured using a base and its perpendicular height."]},
 {id:"TRIANGULAR_BOARD" as const,intros:["A triangular board has a fixed base and perpendicular height.","Consider a triangular board measured by its base and perpendicular height.","The dimensions of a triangular board are being examined.","A triangular panel is measured using a base and its perpendicular height."]},
] as const;
const RECTANGLE_CONTEXTS=[
 {id:"RECTANGULAR_FIELD" as const,intros:["A rectangular field has fixed length and breadth.","Consider a rectangular field with perpendicular length and breadth.","The dimensions of a rectangular field are being examined.","A rectangular field is measured by its length and breadth."]},
 {id:"RECTANGULAR_FLOOR" as const,intros:["A rectangular floor has fixed length and breadth.","Consider a rectangular floor with perpendicular length and breadth.","The dimensions of a rectangular floor are being examined.","A rectangular floor is measured by its length and breadth."]},
] as const;
const CIRCLE_CONTEXTS=[
 {id:"CIRCULAR_GARDEN" as const,intros:["A circular garden has a fixed radius.","Consider a circular garden whose radius is being measured.","The dimensions of a circular garden are under consideration.","A circular garden is described by its radius."]},
 {id:"CIRCULAR_TRACK" as const,intros:["A circular track has a fixed radius.","Consider a circular track whose radius is being measured.","The dimensions of a circular track are under consideration.","A circular track is described by its radius."]},
] as const;
const PYRAMID_CONTEXTS=[
 {id:"PYRAMID_MODEL" as const,intros:["A square-pyramid model has a square base and vertical height.","Consider a square pyramid with a base side and perpendicular height.","The dimensions of a square-pyramid model are being examined.","A square pyramid is described by its base side and perpendicular height."]},
 {id:"PYRAMID_STRUCTURE" as const,intros:["A square-pyramid structure has a square base and vertical height.","Consider a square-pyramid structure with a base side and perpendicular height.","The dimensions of a square-pyramid structure are being examined.","A square-pyramid structure is described by its base side and perpendicular height."]},
] as const;
const FRUSTUM_CONTEXTS=[
 {id:"FRUSTUM_CONTAINER" as const,intros:["A conical-frustum container has two circular ends and a perpendicular height.","Consider a conical frustum described by its two radii and height.","The dimensions of a conical-frustum container are being examined.","A frustum-shaped container is measured by its two radii and perpendicular height."]},
 {id:"FRUSTUM_MODEL" as const,intros:["A conical-frustum model has two circular ends and a perpendicular height.","Consider a conical-frustum model described by its two radii and height.","The dimensions of a conical-frustum model are being examined.","A frustum-shaped model is measured by its two radii and perpendicular height."]},
] as const;

function hashSeed(seed:number,salt:string){let hash=2166136261;for(const ch of `${DSF_CP011_MENSURATION_RUNTIME_VERSION}:${seed}:${salt}`){hash^=ch.charCodeAt(0);hash=Math.imul(hash,16777619);}return hash>>>0;}
function createRng(seed:number,salt:string){let state=hashSeed(seed,salt)||0x9e3779b9;return()=>{state^=state<<13;state^=state>>>17;state^=state<<5;return(state>>>0)/0x100000000;};}
function pick<T>(random:()=>number,values:readonly T[]):T{return values[Math.floor(random()*values.length)]!;}
function desiredClass(seed:number):SufficiencyClass{return SUFFICIENCY_CLASSES[Math.abs(seed)%SUFFICIENCY_CLASSES.length]!;}

function men001Params(solveMode:Men001SolveMode,values:Men001Parameters["values"],cp:"MEN-CP-001"|"MEN-CP-002"|"MEN-CP-003",taskKind:Men001Parameters["taskKind"],answerDimension:Men001Parameters["answerDimension"],unitPolicy:Men001Parameters["unitPolicy"]):Men001Parameters{
 return{packageId:"MEN-001",canonicalProblemId:cp,questionId:`DSF-CP011-${solveMode}`,questionLanguageId:`DSF-CP011-${solveMode}`,language:"en",difficulty:"Medium",taskKind,solveMode,answerDimension,unitPolicy,seed:"DSF-CP011",values,renderVariables:{}};
}
function solve2d(mode:DsfCp011MensurationSolveMode,world:TwoDWorld):string{
 switch(mode){
  case"DSF-SM-MEN-TRIANGLE-AREA":return solveMen001(men001Params("findTriangleAreaBaseHeight",{base:world.a,height:world.b},"MEN-CP-001","triangleMeasurementApplication","AREA","SQUARE_METRES")).answer;
  case"DSF-SM-MEN-RECTANGLE-AREA":return solveMen001(men001Params("findRectangleArea",{length:world.a,breadth:world.b},"MEN-CP-002","quadrilateralMeasurementApplication","AREA","SQUARE_METRES")).answer;
  case"DSF-SM-MEN-RECTANGLE-PERIMETER":return solveMen001(men001Params("findRectanglePerimeter",{length:world.a,breadth:world.b},"MEN-CP-002","quadrilateralMeasurementApplication","LENGTH","METRES")).answer;
  case"DSF-SM-MEN-CIRCLE-AREA":return solveMen001(men001Params("findCircleAreaFromRadius",{radius:world.a},"MEN-CP-003","circleMeasurementApplication","AREA","SQUARE_METRES")).answer;
  case"DSF-SM-MEN-CIRCLE-CIRCUMFERENCE":return solveMen001(men001Params("findCircleCircumferenceFromRadius",{radius:world.a},"MEN-CP-003","circleMeasurementApplication","LENGTH","METRES")).answer;
  default:throw new Error(`${mode} is not a MEN-001 mode.`);
 }
}
function buildTriangleWorlds():readonly TwoDWorld[]{const worlds:TwoDWorld[]=[];for(const base of [8,10,12,14,16,18,20,24] as const)for(const height of [6,8,10,12,14,16,18,20] as const){const raw:TwoDWorld={kind:"TRIANGLE",a:base,b:height};worlds.push({...raw,area:solve2d("DSF-SM-MEN-TRIANGLE-AREA",raw)});}return worlds;}
function buildRectangleWorlds():readonly TwoDWorld[]{const worlds:TwoDWorld[]=[];for(const length of [12,15,18,20,24,28,30,32] as const)for(const breadth of [6,8,9,10,12,14,16,18] as const){if(length<=breadth)continue;const raw:TwoDWorld={kind:"RECTANGLE",a:length,b:breadth};worlds.push({...raw,area:solve2d("DSF-SM-MEN-RECTANGLE-AREA",raw),perimeter:solve2d("DSF-SM-MEN-RECTANGLE-PERIMETER",raw)});}return worlds;}
function buildCircleWorlds():readonly TwoDWorld[]{return([7,14,21,28,35,42,49,56] as const).map(radius=>{const raw:TwoDWorld={kind:"CIRCLE",a:radius};return{...raw,area:solve2d("DSF-SM-MEN-CIRCLE-AREA",raw),circumference:solve2d("DSF-SM-MEN-CIRCLE-CIRCUMFERENCE",raw)};});}

function pyramidState(side:number,height:number):MenCp010State{const t=buildMenCp010State("MEN-CP010-PROT-SQUARE-PYRAMID-VOLUME",`DSF-CP011-PYR-${side}-${height}`);return{...t,seed:`DSF-CP011-PYR-${side}-${height}`,piPolicy:null,unit:"m³",dimensions:{side:BigInt(side),height:BigInt(height)},derived:{},contextId:"square_pyramid"};}
function frustumState(innerRadius:number,outerRadius:number,height:number):MenCp010State{const t=buildMenCp010State("MEN-CP010-PROT-CONICAL-FRUSTUM-VOLUME",`DSF-CP011-FR-${innerRadius}-${outerRadius}-${height}`);return{...t,seed:`DSF-CP011-FR-${innerRadius}-${outerRadius}-${height}`,piPolicy:"EXACT_PI",unit:"m³",dimensions:{innerRadius:BigInt(innerRadius),outerRadius:BigInt(outerRadius),radiusDifference:BigInt(outerRadius-innerRadius),height:BigInt(height)},derived:{},contextId:"conical_frustum"};}
function solidWorld(kind:SolidWorld["kind"],state:MenCp010State):SolidWorld{const answer=solveMenCp010(state);return{kind,state,answerKey:`${exactKey(answer)}|${state.unit}`,answerDisplay:formatWithUnit(answer,state.unit)};}
function buildPyramidWorlds():readonly SolidWorld[]{const worlds:SolidWorld[]=[];for(const side of [4,6,8,10,12,14] as const)for(const height of [3,6,9,12,15,18] as const)worlds.push(solidWorld("SQUARE_PYRAMID",pyramidState(side,height)));return worlds;}
function buildFrustumWorlds():readonly SolidWorld[]{const worlds:SolidWorld[]=[];for(const inner of [2,3,4,5,6] as const)for(const outer of [7,8,9,10,12] as const)for(const height of [3,6,9,12,15] as const)worlds.push(solidWorld("CONICAL_FRUSTUM",frustumState(inner,outer,height)));return worlds;}
const TRIANGLE_WORLDS=buildTriangleWorlds(),RECTANGLE_WORLDS=buildRectangleWorlds(),CIRCLE_WORLDS=buildCircleWorlds(),PYRAMID_WORLDS=buildPyramidWorlds(),FRUSTUM_WORLDS=buildFrustumWorlds();

function sourceAnswer(problem:MensurationProblem,world:MensurationWorld):string{if(world.kind==="SQUARE_PYRAMID"||world.kind==="CONICAL_FRUSTUM"){const answer=solveMenCp010(world.state);return`${exactKey(answer)}|${world.state.unit}`;}return solve2d(problem.solveMode,world);}
function baseWorlds(problem:MensurationProblem):readonly MensurationWorld[]{switch(problem.solveMode){case"DSF-SM-MEN-TRIANGLE-AREA":return TRIANGLE_WORLDS;case"DSF-SM-MEN-RECTANGLE-AREA":case"DSF-SM-MEN-RECTANGLE-PERIMETER":return RECTANGLE_WORLDS;case"DSF-SM-MEN-CIRCLE-AREA":case"DSF-SM-MEN-CIRCLE-CIRCUMFERENCE":return CIRCLE_WORLDS;case"DSF-SM-MEN-SQUARE-PYRAMID-VOLUME":return PYRAMID_WORLDS;case"DSF-SM-MEN-CONICAL-FRUSTUM-VOLUME":return FRUSTUM_WORLDS;}}
const adapter={adapterId:"DSF-CP011-MENSURATION-SOURCE-BOUND",domainFamily:"QUANT" as const,enumerateBaseWorlds:baseWorlds,statementHolds:(_problem:MensurationProblem,world:MensurationWorld,statement:MensurationStatement)=>statement.test(world),evaluateTarget:(problem:MensurationProblem,world:MensurationWorld)=>sourceAnswer(problem,world),normalizeAnswer:(answer:string)=>answer};
function st(id:string,family:StatementFamily,complexity:1|2|3,text:string,test:(world:MensurationWorld)=>boolean):MensurationStatement{return{id,family,complexity,text,test};}

function twoDStatements(problem:MensurationProblem,a:TwoDWorld):readonly MensurationStatement[]{
 if(a.kind==="TRIANGLE"){const base=a.a,height=a.b!,area=a.area!;return[
  st(`B_${base}`,"BASE_EXACT",1,`The base is ${base} metres.`,w=>w.kind==="TRIANGLE"&&w.a===base),st(`H_${height}`,"HEIGHT_EXACT",1,`The perpendicular height is ${height} metres.`,w=>w.kind==="TRIANGLE"&&w.b===height),st(`A_${area}`,"AREA_EXACT",1,`The area is ${area}.`,w=>w.kind==="TRIANGLE"&&w.area===area),st(`BH_${base}_${height}`,"BASE_HEIGHT_PAIR",2,`The base is ${base} m and the perpendicular height is ${height} m.`,w=>w.kind==="TRIANGLE"&&w.a===base&&w.b===height),st(`BLE_${base}`,"DIMENSION_BOUND",2,`The base is at most ${base} metres.`,w=>w.kind==="TRIANGLE"&&w.a<=base),st(`HGE_${height}`,"DIMENSION_BOUND",2,`The height is at least ${height} metres.`,w=>w.kind==="TRIANGLE"&&(w.b??0)>=height)];}
 if(a.kind==="RECTANGLE"){const length=a.a,breadth=a.b!,area=a.area!,perimeter=a.perimeter!,isArea=problem.solveMode==="DSF-SM-MEN-RECTANGLE-AREA",target=isArea?area:perimeter;return[
  st(`L_${length}`,"LENGTH_EXACT",1,`The length is ${length} metres.`,w=>w.kind==="RECTANGLE"&&w.a===length),st(`BR_${breadth}`,"BREADTH_EXACT",1,`The breadth is ${breadth} metres.`,w=>w.kind==="RECTANGLE"&&w.b===breadth),st(`T_${target}`,isArea?"AREA_EXACT":"PERIMETER_EXACT",1,`The ${isArea?"area":"perimeter"} is ${target}.`,w=>w.kind==="RECTANGLE"&&(isArea?w.area:w.perimeter)===target),st(`LB_${length}_${breadth}`,"LENGTH_BREADTH_PAIR",2,`The length is ${length} m and the breadth is ${breadth} m.`,w=>w.kind==="RECTANGLE"&&w.a===length&&w.b===breadth),st(`LLE_${length}`,"DIMENSION_BOUND",2,`The length is at most ${length} metres.`,w=>w.kind==="RECTANGLE"&&w.a<=length),st(`BGE_${breadth}`,"DIMENSION_BOUND",2,`The breadth is at least ${breadth} metres.`,w=>w.kind==="RECTANGLE"&&(w.b??0)>=breadth)];}
 const radius=a.a,sorted=CIRCLE_WORLDS.map(w=>w.a).sort((x,y)=>x-y),index=sorted.indexOf(radius),lower=index>0?sorted[index-1]!:0,upper=index<sorted.length-1?sorted[index+1]!:radius+14,isArea=problem.solveMode==="DSF-SM-MEN-CIRCLE-AREA",target=isArea?a.area!:a.circumference!;
 return[
  st(`R_${radius}`,"RADIUS_EXACT",1,`The radius is ${radius} metres.`,w=>w.kind==="CIRCLE"&&w.a===radius),st(`D_${2*radius}`,"DIAMETER_EXACT",1,`The diameter is ${2*radius} metres.`,w=>w.kind==="CIRCLE"&&w.a===radius),st(`T_${target}`,isArea?"AREA_EXACT":"CIRCUMFERENCE_EXACT",1,`The ${isArea?"area":"circumference"} is ${target}.`,w=>w.kind==="CIRCLE"&&(isArea?w.area:w.circumference)===target),st(`RL_${lower}`,"RADIUS_WINDOW_LOWER",2,`The radius is greater than ${lower} metres.`,w=>w.kind==="CIRCLE"&&w.a>lower),st(`RU_${upper}`,"RADIUS_WINDOW_UPPER",2,`The radius is less than ${upper} metres.`,w=>w.kind==="CIRCLE"&&w.a<upper),st("R_MIN_7","DIMENSION_BOUND",2,"The radius is at least 7 metres.",w=>w.kind==="CIRCLE"&&w.a>=7),st(`RP_${radius%2}`,"RADIUS_PARITY",2,`The radius in metres is ${radius%2===0?"even":"odd"}.`,w=>w.kind==="CIRCLE"&&w.a%2===radius%2)];
}
function solidStatements(a:SolidWorld):readonly MensurationStatement[]{const d=a.state.dimensions;if(a.kind==="SQUARE_PYRAMID"){const side=Number(d.side),height=Number(d.height);return[
 st(`PS_${side}`,"PYRAMID_SIDE_EXACT",1,`The square base has side ${side} m.`,w=>w.kind==="SQUARE_PYRAMID"&&w.state.dimensions.side===d.side),st(`PH_${height}`,"PYRAMID_HEIGHT_EXACT",1,`The perpendicular height is ${height} m.`,w=>w.kind==="SQUARE_PYRAMID"&&w.state.dimensions.height===d.height),st(`PV_${a.answerKey}`,"PYRAMID_VOLUME_EXACT",1,`The volume is ${a.answerDisplay}.`,w=>w.kind==="SQUARE_PYRAMID"&&w.answerKey===a.answerKey),st(`PSH_${side}_${height}`,"PYRAMID_SIDE_HEIGHT_PAIR",2,`The base side is ${side} m and the perpendicular height is ${height} m.`,w=>w.kind==="SQUARE_PYRAMID"&&w.state.dimensions.side===d.side&&w.state.dimensions.height===d.height),st(`PSLE_${side}`,"SOLID_BOUND",2,`The base side is at most ${side} m.`,w=>w.kind==="SQUARE_PYRAMID"&&Number(w.state.dimensions.side)<=side),st(`PHGE_${height}`,"SOLID_BOUND",2,`The height is at least ${height} m.`,w=>w.kind==="SQUARE_PYRAMID"&&Number(w.state.dimensions.height)>=height)];}
 const outer=Number(d.outerRadius),inner=Number(d.innerRadius),height=Number(d.height);return[
  st(`FO_${outer}`,"OUTER_RADIUS_EXACT",1,`The outer radius is ${outer} m.`,w=>w.kind==="CONICAL_FRUSTUM"&&w.state.dimensions.outerRadius===d.outerRadius),st(`FI_${inner}`,"INNER_RADIUS_EXACT",1,`The inner radius is ${inner} m.`,w=>w.kind==="CONICAL_FRUSTUM"&&w.state.dimensions.innerRadius===d.innerRadius),st(`FR_${outer}_${inner}`,"FRUSTUM_RADII_PAIR",2,`The outer and inner radii are ${outer} m and ${inner} m.`,w=>w.kind==="CONICAL_FRUSTUM"&&w.state.dimensions.outerRadius===d.outerRadius&&w.state.dimensions.innerRadius===d.innerRadius),st(`FH_${height}`,"FRUSTUM_HEIGHT_EXACT",1,`The perpendicular height is ${height} m.`,w=>w.kind==="CONICAL_FRUSTUM"&&w.state.dimensions.height===d.height),st(`FV_${a.answerKey}`,"FRUSTUM_VOLUME_EXACT",1,`The volume is ${a.answerDisplay}.`,w=>w.kind==="CONICAL_FRUSTUM"&&w.answerKey===a.answerKey),st(`FF_${outer}_${inner}_${height}`,"FRUSTUM_FULL_DATA",3,`The outer radius is ${outer} m, inner radius is ${inner} m, and perpendicular height is ${height} m.`,w=>w.kind==="CONICAL_FRUSTUM"&&w.state.dimensions.outerRadius===d.outerRadius&&w.state.dimensions.innerRadius===d.innerRadius&&w.state.dimensions.height===d.height),st(`FOLE_${outer}`,"SOLID_BOUND",2,`The outer radius is at most ${outer} m.`,w=>w.kind==="CONICAL_FRUSTUM"&&Number(w.state.dimensions.outerRadius)<=outer),st(`FHGE_${height}`,"SOLID_BOUND",2,`The height is at least ${height} m.`,w=>w.kind==="CONICAL_FRUSTUM"&&Number(w.state.dimensions.height)>=height)];}
function statementPool(problem:MensurationProblem):readonly MensurationStatement[]{return problem.anchor.kind==="SQUARE_PYRAMID"||problem.anchor.kind==="CONICAL_FRUSTUM"?solidStatements(problem.anchor):twoDStatements(problem,problem.anchor);}
function pairQuality(first:MensurationStatement,second:MensurationStatement,e:TwoStatementSufficiencyEvaluation<string>){return(first.family===second.family?-5:5)+(e.classification==="BOTH_TOGETHER_ONLY"?8:e.classification==="INSUFFICIENT_EVEN_TOGETHER"?3:0)+Math.floor((Math.min(e.statementI.worldCount,50)+Math.min(e.statementII.worldCount,50))/20)-first.complexity-second.complexity;}
function synthesizePair(problem:MensurationProblem,seed:number,targetClass:SufficiencyClass):SynthesizedPair{const candidates:SynthesizedPair[]=[],statements=statementPool(problem);for(const statementI of statements)for(const statementII of statements){if(statementI.id===statementII.id)continue;try{const evaluation=evaluateFiniteDomainPair(adapter,problem,statementI,statementII);if(evaluation.classification===targetClass)candidates.push({statementI,statementII,evaluation,qualityScore:pairQuality(statementI,statementII,evaluation)});}catch{}}if(!candidates.length)throw new Error(`No Mensuration pair for ${problem.solveMode}/${targetClass}`);const best=Math.max(...candidates.map(c=>c.qualityScore));return pick(createRng(seed,`pair:${problem.solveMode}:${targetClass}`),candidates.filter(c=>c.qualityScore>=best-2));}

function buildProblem(seed:number,attempt:number):MensurationProblem{const solveMode=DSF_CP011_MENSURATION_SOLVE_MODES[Math.abs(seed)%DSF_CP011_MENSURATION_SOLVE_MODES.length]!,random=createRng(seed+attempt*65537,`problem:${solveMode}`);if(solveMode==="DSF-SM-MEN-TRIANGLE-AREA"){const c=pick(random,TRIANGLE_CONTEXTS);return{solveMode,targetKind:"AREA",anchor:pick(random,TRIANGLE_WORLDS),contextId:c.id,intro:pick(random,c.intros)};}if(solveMode==="DSF-SM-MEN-RECTANGLE-AREA"||solveMode==="DSF-SM-MEN-RECTANGLE-PERIMETER"){const c=pick(random,RECTANGLE_CONTEXTS);return{solveMode,targetKind:solveMode.endsWith("AREA")?"AREA":"PERIMETER",anchor:pick(random,RECTANGLE_WORLDS),contextId:c.id,intro:pick(random,c.intros)};}if(solveMode==="DSF-SM-MEN-CIRCLE-AREA"||solveMode==="DSF-SM-MEN-CIRCLE-CIRCUMFERENCE"){const c=pick(random,CIRCLE_CONTEXTS);return{solveMode,targetKind:solveMode.endsWith("AREA")?"AREA":"CIRCUMFERENCE",anchor:pick(random,CIRCLE_WORLDS),contextId:c.id,intro:pick(random,c.intros)};}if(solveMode==="DSF-SM-MEN-SQUARE-PYRAMID-VOLUME"){const c=pick(random,PYRAMID_CONTEXTS);return{solveMode,targetKind:"VOLUME",anchor:pick(random,PYRAMID_WORLDS),contextId:c.id,intro:pick(random,c.intros)};}const c=pick(random,FRUSTUM_CONTEXTS);return{solveMode,targetKind:"VOLUME",anchor:pick(random,FRUSTUM_WORLDS),contextId:c.id,intro:pick(random,c.intros)};}
function targetPrompt(problem:MensurationProblem){switch(problem.solveMode){case"DSF-SM-MEN-TRIANGLE-AREA":return"What is the area of the triangle?";case"DSF-SM-MEN-RECTANGLE-AREA":return"What is the area of the rectangle?";case"DSF-SM-MEN-RECTANGLE-PERIMETER":return"What is the perimeter of the rectangle?";case"DSF-SM-MEN-CIRCLE-AREA":return"What is the area of the circle?";case"DSF-SM-MEN-CIRCLE-CIRCUMFERENCE":return"What is the circumference of the circle?";case"DSF-SM-MEN-SQUARE-PYRAMID-VOLUME":return"What is the volume of the square pyramid?";case"DSF-SM-MEN-CONICAL-FRUSTUM-VOLUME":return"What is the volume of the conical frustum?";}}
function explanation(label:string,answers:readonly string[],sufficient:boolean){if(sufficient)return`${label} fixes the asked value at ${answers[0]}. Therefore, ${label} alone is sufficient.`;const e=answers.slice(0,2);return e.length>=2?`${label} permits at least two different answers, ${e[0]} and ${e[1]}. Therefore, ${label} alone is not sufficient.`:`${label} does not fix a unique answer. Therefore, ${label} alone is not sufficient.`;}
function difficultyFor(pair:SynthesizedPair):DsfCp011MensurationDifficulty{if(pair.evaluation.classification==="BOTH_TOGETHER_ONLY"||pair.evaluation.classification==="INSUFFICIENT_EVEN_TOGETHER")return"Hard";return pair.statementI.complexity===1&&pair.statementII.complexity===1?"Easy":"Medium";}
export function normalizeDsfCp011MensurationSurface(text:string){return text.toLowerCase().replace(/\d+(?:\.\d+)?(?:\/\d+)?/g,"#").replace(/[^a-z#]+/g," ").trim().replace(/\s+/g," ");}
export function generateDsfCp011MensurationQuestion(seed:number){const targetClass=desiredClass(seed);let problem:MensurationProblem|undefined,pair:SynthesizedPair|undefined,lastError:unknown;for(let attempt=0;attempt<48;attempt+=1){const candidate=buildProblem(seed,attempt);try{pair=synthesizePair(candidate,seed+attempt*104729,targetClass);problem=candidate;break;}catch(error){lastError=error;}}if(!problem||!pair)throw lastError instanceof Error?lastError:new Error(`Unable to synthesize Mensuration DS question for seed ${seed}`);const e=pair.evaluation,prompt=targetPrompt(problem),stem=`${problem.intro} ${prompt}`,correct=optionForClass(DS_STANDARD_5_EN,e.classification),sourceChapterId=problem.anchor.kind==="SQUARE_PYRAMID"||problem.anchor.kind==="CONICAL_FRUSTUM"?"MEN-002" as const:"MEN-001" as const,sourceCapability=sourceChapterId==="MEN-001"?"MEN-001/solver::solveMen001" as const:"MEN-002/cp010-foundation/engine::{buildMenCp010State,solveMenCp010}" as const,together=!e.statementI.sufficient&&!e.statementII.sufficient?(e.together.sufficient?`Using both statements together fixes the asked value at ${e.together.normalizedTargetAnswers[0]}. So the two statements together are sufficient.`:`Even together, the statements allow different answers such as ${e.together.normalizedTargetAnswers.slice(0,2).join(" and ")}. So they are still insufficient.`):undefined;return Object.freeze({packageId:"DSF-001" as const,checkpointId:"DSF-CP-011" as const,qlId:"DSF-QL-001" as const,runtimeVersion:DSF_CP011_MENSURATION_RUNTIME_VERSION,seed,locale:"en-IN" as const,difficulty:difficultyFor(pair),domainFamily:"QUANT" as const,sourceChapterId,sourceCapability,solveModeId:problem.solveMode,targetKind:problem.targetKind,contextId:problem.contextId,answerContractId:"DS_STANDARD_5" as const,taskDirection:"DATA_SUFFICIENCY" as const,answerSemantic:"SUFFICIENCY_CLASS" as const,stem,questionPrompt:prompt,statements:[{id:"I" as const,statementRuleId:pair.statementI.id,statementFamily:pair.statementI.family,text:pair.statementI.text},{id:"II" as const,statementRuleId:pair.statementII.id,statementFamily:pair.statementII.family,text:pair.statementII.text}] as const,options:DS_STANDARD_5_EN.options.map(option=>({key:option.key,value:option.text,semanticClass:option.semanticClass,isCorrect:option.semanticClass===e.classification})),correctIndex:DS_STANDARD_5_EN.options.findIndex(option=>option.semanticClass===e.classification),canonicalAnswer:e.classification,explanation:{askedTarget:`We need to determine ${prompt.charAt(0).toLowerCase()}${prompt.slice(1)}`,statementI:explanation("Statement I",e.statementI.normalizedTargetAnswers,e.statementI.sufficient),statementII:explanation("Statement II",e.statementII.normalizedTargetAnswers,e.statementII.sufficient),...(together?{together}:{}),conclusion:correct.text},proof:{baseWorldCount:baseWorlds(problem).length,statementIWorldCount:e.statementI.worldCount,statementIIWorldCount:e.statementII.worldCount,togetherWorldCount:e.together.worldCount,statementITargetAnswers:e.statementI.normalizedTargetAnswers,statementIITargetAnswers:e.statementII.normalizedTargetAnswers,togetherTargetAnswers:e.together.normalizedTargetAnswers,minimalSufficientSets:e.minimalSufficientSets,canonicalArithmeticOwnedByDsf:false as const},sourceAncestry:[sourceChapterId,sourceCapability] as const,generationIdentity:createHash("sha256").update(`${DSF_CP011_MENSURATION_RUNTIME_VERSION}|${seed}|${problem.solveMode}|${problem.contextId}|${pair.statementI.id}|${pair.statementII.id}`).digest("hex").slice(0,24),studentSurfaceFingerprint:`${normalizeDsfCp011MensurationSurface(stem)}|${problem.solveMode}|${pair.statementI.family}|${pair.statementII.family}`,lifecycle:{contentStatus:"CP011_EXPANSION_REVIEW_CANDIDATE" as const,questionStudioDiscoverable:false as const,questionBankWritable:false as const,testEligible:false as const,publiclyPublishable:false as const}});}
export function generateDsfCp011MensurationBatch(seeds:readonly number[]){return seeds.map(generateDsfCp011MensurationQuestion);}
