# DSF-001 Data Sufficiency — Chapter Manifest

Status: `DSF-CP-000 FROZEN / DSF-QL-001 ALLOCATED`

## Identity

- Product chapter: Data Sufficiency
- Chapter code: `REAS-DSF`
- Implementation package: `DSF-001`
- Frozen checkpoint: `DSF-CP-000`
- Freeze authority: `DSF_CP000_FOUNDATION_DISCOVERY_FREEZE_V1`
- First permanent QL: `DSF-QL-001`
- Next available QL ID: `DSF-QL-002`
- Question Studio publication: **locked**

## Frozen semantic rule

A statement set is sufficient when at least one valid world remains and every surviving valid world gives the same normalized answer to the asked target. Complete-world uniqueness is not required.

Statement I, Statement II, and their conjunction are evaluated independently from the same base problem.

Canonical two-statement classes:

- `STATEMENT_I_ONLY`
- `STATEMENT_II_ONLY`
- `EACH_STATEMENT_ALONE`
- `BOTH_TOGETHER_ONLY`
- `INSUFFICIENT_EVEN_TOGETHER`

## Permanent QL allocation

`DSF-QL-001 — TWO_STATEMENT_TARGET_DETERMINACY`

Answer semantic: `SUFFICIENCY_CLASS`.

Source chapter and target kind are solve-mode / adapter metadata, not separate permanent QLs by themselves. The frozen contract covers scalar/exact values, categorical/yes-no properties, ranks/positions, identities, directions/distances, relations, comparisons and counts whenever the source adapter can completely determine the valid target-answer set.

## CP-000 evidence frozen

- shared target-projection evaluator and invariants;
- exhaustive finite-domain property sweep;
- Number System source-capability reuse with all five classes;
- Algebra target-function proof with multiple complete `(x,y)` worlds but unique `x+y`;
- Ranking & Order proof with multiple total orders but one unique asked rank;
- Banking five-option and reordered five-option source profiles;
- Banking three-statement subset patterns;
- SSC CGL four-option source profiles;
- PSSSB Clerk preparation signal retained as partial Punjab evidence;
- NUM/TMW/SAP/RNK ownership reconciliation;
- merge/split and QL-boundary audit.

## Ownership

- DSF owns statement isolation, conjunction, target projection, sufficiency classification and DS answer contracts.
- Source chapters retain mathematical/reasoning truth and source-domain solving.
- Frozen source QLs such as `TMW-QL-216..223` are preserved and are not renumbered or cloned.

## Deferred contracts

The following are not part of `DSF-QL-001` production rollout yet:

- `DSF-QL-CAND-002 / THREE_STATEMENT_MINIMAL_SUFFICIENT_SUBSETS`;
- Seating and general-puzzle DS until complete source-world interfaces are proven;
- Punjab-specific answer rendering until an official-paper profile is verified.

## Lifecycle after allocation

Permanent identity allocation does not publish content.

```text
Question Studio discoverable: false
Question Bank writable:       false
mock-test eligible:            false
publicly publishable:          false
```

## Next checkpoint

`DSF-CP-001` implements production generation for `DSF-QL-001`, beginning with source-backed Quant adapters and deterministic question/explanation generation. Product registration remains a later gated step.
