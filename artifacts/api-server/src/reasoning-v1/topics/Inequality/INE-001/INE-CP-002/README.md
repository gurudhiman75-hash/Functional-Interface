# INE-CP-002 — Multi-Link, Branched, and Disconnected Graphs

- **Status:** Executable prototype discovery in progress
- **Permanent QLs:** `0`
- **Frozen solve modes:** `0`
- **Question Studio visibility:** Disabled
- **Public release:** Disabled

This checkpoint extends the CP-001 inequality foundation to larger evidence graphs without changing the canonical comparison semantics.

The current executable discovery covers:

- long chains with strict, inclusive, and equality links;
- two consistent routes between the same pair;
- a non-strict direct route strengthened by a strict alternate route;
- branch terms whose mutual order remains unknown;
- irrelevant statements that must not affect the queried relation;
- selecting the only pair with a definite relation;
- selecting the only pair without a definite relation;
- queries across disconnected non-trivial components;
- equality components that span multiple branches;
- natural, question-specific English explanations;
- complete relation and pair-option validation against both solvers.

All nine authorities are provisional discovery labels. Representation-only variants and closely related graph operations remain open to merge/split review.

No permanent QL, multilingual runtime, central registry entry, or release toggle is introduced by this checkpoint.

## Next discovery work

1. Manually review the exported English pack.
2. Complete page-level source mapping when the synced library material is available.
3. Decide which graph-shape authorities merge into shared permanent solve contracts.
4. Request approval before allocating permanent QLs.
