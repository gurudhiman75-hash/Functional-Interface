# BLR-CP-007 Editorial V3 Plan

Status: implementation scaffold only.

V3 retains the five permanent QLs and the validated coded-family graph solver, but replaces the V2 token-reskin bank with semantic scenario pools.

Required gates:

- at least eight materially distinct family constructions within every legacy prototype family;
- balanced male, female and gender-neutral target coverage;
- all QL-034 candidates participate in meaningful clues;
- no candidate can be selected by absence or string matching;
- QL-033 stems specify the final kinship target without revealing both local links;
- one-to-one token-to-relation mappings in every key;
- exam-facing token-style distribution led by symbols and capital-letter operators;
- difficulty derived from reasoning depth, not token style;
- no duplicated `Therefore` renderer prefix;
- validity-option explanations decode the actual option before applying polarity;
- internal diagnostic codes remain administrator-only;
- diagrams are optional for direct lookup and required for multi-link/affinal explanations;
- standalone and shared-set delivery contracts are represented.

This file does not record a freeze or approval.