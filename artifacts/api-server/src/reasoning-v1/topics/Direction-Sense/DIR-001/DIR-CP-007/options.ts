import type{CardinalDirection}from"../foundation/types";import{CARDINAL_DIRECTIONS,SUN_SHADOW_DIRECTION_LABELS,PERIOD_LABELS}from"./environmental-orientation";import type{RelativeShadowSide,SunShadowAnswer,SunShadowOption,SunTimePeriod,TimeAnswerPeriod}from"./types";
function random(seed:number){let s=(seed^0x9e3779b9)>>>0;return()=>{s+=0x6d2b79f5;let v=s;v=Math.imul(v^(v>>>15),v|1);v^=v+Math.imul(v^(v>>>7),v|61);return((v^(v>>>14))>>>0)/4294967296;};}function shuffle<T>(a:readonly T[],seed:number){const r=random(seed),out=[...a];for(let i=out.length-1;i>0;i--){const j=Math.floor(r()*(i+1));[out[i],out[j]]=[out[j],out[i]];}return out;}
export function directionOptions(c:CardinalDirection,seed:number):readonly SunShadowOption[]{return shuffle(CARDINAL_DIRECTIONS.map(d=>({value:{kind:"DIRECTION"as const,direction:d},label:SUN_SHADOW_DIRECTION_LABELS[d],errorLabel:d===c?null:"SUN_SHADOW_OR_SIDE_CONFUSED"})),seed*41+7);}
const SL:Readonly<Record<RelativeShadowSide,string>>={LEFT:"To the left",RIGHT:"To the right",FRONT:"In front",BEHIND:"Behind"};export function sideOptions(c:RelativeShadowSide,seed:number):readonly SunShadowOption[]{return shuffle((["LEFT","RIGHT","FRONT","BEHIND"]as const).map(s=>({value:{kind:"RELATIVE_SIDE"as const,side:s},label:SL[s],errorLabel:s===c?null:"PERSON_REFERENCE_FRAME_ROTATED"})),seed*43+11);}
const TL:Readonly<Record<TimeAnswerPeriod,string>>={MORNING:PERIOD_LABELS.MORNING,EVENING:PERIOD_LABELS.EVENING,NOON:"Noon",CANNOT_BE_DETERMINED:"Cannot be determined"};export function timeOptions(c:SunTimePeriod,seed:number):readonly SunShadowOption[]{return shuffle((["MORNING","EVENING","NOON","CANNOT_BE_DETERMINED"]as const).map(p=>({value:{kind:"TIME_PERIOD"as const,period:p},label:TL[p],errorLabel:p===c?null:"TIME_CONVENTION_MISREAD"})),seed*47+13);}
export function answerKey(a:SunShadowAnswer):string{return a.kind==="DIRECTION"?`D:${a.direction}`:a.kind==="RELATIVE_SIDE"?`S:${a.side}`:`T:${a.period}`;}
