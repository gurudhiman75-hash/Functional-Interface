# RNK-CP-001 — Final Inverse-Gap Addendum

Status: **open executable discovery; no permanent QL allocation authorised**.

## Gap found during consolidation

The first two executable waves cover:

- start-rank from total and count after;
- count before from total and end-rank;
- same-side rank/count conversions on both ends.

One mirrored inverse remained absent:

```text
given total people + count before the target
ask rank from the end
```

The exact equation is:

```text
rank from end = total people − people before
```

This is not the same displayed task as start-rank from count before, because the requested end changes and the total is necessary.

## New provisional prototype

```text
RNK-CP001-PROT-END-RANK-FROM-COUNT-BEFORE-AND-TOTAL
```

It remains a discovery identity. The later merge/split audit may merge it with the mirrored start-rank authority through an end-parameterised contract.

## Proof target

```text
1 prototype × 240 seeds = 240 questions
```

Required checks:

- deterministic replay;
- canonical and independent solver agreement;
- total-order invariants;
- first/last boundary coverage;
- merit-list, row and queue contexts;
- all four answer positions;
- Easy, Medium and Hard coverage;
- unique options and one correct answer;
- value-specific teaching;
- proper-name and number-agreement safety;
- complete lifecycle locks.

After this addendum, the CP-001 executable discovery total becomes:

```text
first foundation wave:             1,440
source and inverse-gap wave:       1,440
final mirrored inverse addendum:     240
----------------------------------------
combined CP-001 discovery:         3,120
```

Permanent QLs remain zero.
