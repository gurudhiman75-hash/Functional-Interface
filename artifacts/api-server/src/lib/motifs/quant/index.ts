import { fundamentalsMotifs } from "./fundamentals";
import { algebraMotifs } from "./algebra";
import { coordinateGeometryMotifs } from "./coordinate-geometry";
import { equationsMotifs } from "./equations";
import { functionsMotifs } from "./functions";
import { numberSystemMotifs } from "./number-system";
import { numericalPropertyMotifs } from "./number-system-advanced";
import { algebraBasicsMotifs } from "./algebra-basics";
import { averageBalanceMotifs } from "./averages-advanced";
import { averagesMotifs } from "./averages";
import { geometryMotifs } from "./geometry";
import { mensurationMotifs } from "./mensuration";
import { mixtureAlligationMotifs } from "./mixture-alligation";
import { percentageMotifs } from "./percentage";
import { permutationCombinationMotifs } from "./permutation-combination";
import { progressionsMotifs } from "./progressions";
import { probabilityMotifs } from "./probability";
import { profitLossMotifs } from "./profit-loss";
import { ratioProportionMotifs } from "./ratio-proportion";
import { setTheoryMotifs } from "./set-theory";
import {
  compoundInterestMotifs,
  simpleCompoundInterestMotifs,
  simpleInterestMotifs,
} from "./simple-compound-interest";
import { simplificationMotifs } from "./simplification";
import { speedDistanceMotifs } from "./speed-distance";
import { timeWorkMotifs } from "./time-work";
import { trigonometryMotifs } from "./trigonometry";

export const quantTopicMotifs = [
  ...fundamentalsMotifs,
  ...simplificationMotifs,
  ...numberSystemMotifs,
  ...numericalPropertyMotifs,
  ...percentageMotifs,
  ...ratioProportionMotifs,
  ...averagesMotifs,
  ...averageBalanceMotifs,
  ...profitLossMotifs,
  ...simpleCompoundInterestMotifs,
  ...timeWorkMotifs,
  ...speedDistanceMotifs,
  ...mixtureAlligationMotifs,
  ...algebraMotifs,
  ...equationsMotifs,
  ...progressionsMotifs,
  ...probabilityMotifs,
  ...functionsMotifs,
  ...algebraBasicsMotifs,
  ...permutationCombinationMotifs,
  ...trigonometryMotifs,
  ...geometryMotifs,
  ...coordinateGeometryMotifs,
  ...setTheoryMotifs,
  ...mensurationMotifs,
];

export {
  fundamentalsMotifs,
  simplificationMotifs,
  algebraMotifs,
  coordinateGeometryMotifs,
  equationsMotifs,
  functionsMotifs,
  numberSystemMotifs,
  numericalPropertyMotifs,
  percentageMotifs,
  permutationCombinationMotifs,
  progressionsMotifs,
  probabilityMotifs,
  ratioProportionMotifs,
  setTheoryMotifs,
  averagesMotifs,
  averageBalanceMotifs,
  profitLossMotifs,
  simpleCompoundInterestMotifs,
  simpleInterestMotifs,
  compoundInterestMotifs,
  timeWorkMotifs,
  speedDistanceMotifs,
  mixtureAlligationMotifs,
  algebraBasicsMotifs,
  geometryMotifs,
  mensurationMotifs,
  trigonometryMotifs,
};
