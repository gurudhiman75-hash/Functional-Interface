# Localization Tests

The executable multilingual regression suite lives in
`src/quant-v2/tests/multilingual-realization.test.ts` so it can run with the
existing quant-v2 test harness. This folder is reserved for language-specific
test fixtures as the localization layer expands.

