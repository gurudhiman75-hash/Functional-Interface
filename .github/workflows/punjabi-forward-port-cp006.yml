name: Validate Punjabi Forward Port CP006

on:
  push:
    branches:
      - "feature/punjabi-forward-port-cp006-v1"
    paths:
      - "artifacts/api-server/src/punjabi-v1/**"
      - ".github/workflows/punjabi-forward-port-cp006.yml"
  pull_request:
    paths:
      - "artifacts/api-server/src/punjabi-v1/**"
      - ".github/workflows/punjabi-forward-port-cp006.yml"
  workflow_dispatch:

jobs:
  validate-cp006:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Bundle CP006 semantic proof
        working-directory: artifacts/api-server
        run: >-
          pnpm exec esbuild
          src/punjabi-v1/packages/PUN-001/checkpoints/CP006/CP006.test.ts
          --bundle --platform=node --format=esm
          --outfile=dist/punjabi-forward-port-cp006.test.mjs
      - name: Run CP006 semantic proof
        working-directory: artifacts/api-server
        run: node dist/punjabi-forward-port-cp006.test.mjs
      - name: Build API server
        working-directory: artifacts/api-server
        run: pnpm run build
