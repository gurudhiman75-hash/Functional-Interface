name: Validate Science CP006 CP007 Review

on:
  pull_request:
    branches:
      - New-main
    paths:
      - "artifacts/api-server/src/knowledge-v1/science/sound/**"
      - "artifacts/api-server/src/knowledge-v1/science/light-optics/**"
      - "artifacts/api-server/src/knowledge-v1/science/sci-cp006-cp007-*"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: science-cp006-cp007-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: corepack enable
      - run: pnpm install --frozen-lockfile
      - name: Run qualification gate
        run: |
          mkdir -p artifacts/api-server/dist/science
          pnpm --dir artifacts/api-server exec esbuild \
            src/knowledge-v1/science/sci-cp006-cp007-review-v1.test.ts \
            --bundle --packages=external --platform=node --format=esm \
            --outfile=dist/science/sci-cp006-cp007-review-v1.test.mjs
          node artifacts/api-server/dist/science/sci-cp006-cp007-review-v1.test.mjs
      - name: Materialize review artifact
        run: |
          pnpm --dir artifacts/api-server exec esbuild \
            src/knowledge-v1/science/sci-cp006-cp007-export-review-v1.ts \
            --bundle --packages=external --platform=node --format=esm \
            --outfile=dist/science/sci-cp006-cp007-export-review-v1.mjs
          cd artifacts/api-server
          node dist/science/sci-cp006-cp007-export-review-v1.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: SCI-CP006-CP007-REVIEW-V1
          path: artifacts/api-server/dist/science-review/SCI-CP006-CP007-V1/
          if-no-files-found: error
      - name: Build API server
        run: pnpm --dir artifacts/api-server build
