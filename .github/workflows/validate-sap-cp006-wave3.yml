name: Validate SAP-CP-006 exact-arithmetic data sufficiency

on:
  pull_request:
    branches:
      - New-main
      - feat/sap-cp005-structural-cancellation-foundation
    types: [opened, reopened, synchronize]
    paths:
      - artifacts/api-server/src/quant-v4/topics/Arithmetic/subtopics/SimplificationAndApproximation/SAP-001/SAP-CP-006/**
      - .github/workflows/validate-sap-cp006-wave3.yml
      - package.json
      - pnpm-lock.yaml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validate-sap-cp006-wave3-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-sap-cp006-wave3:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: corepack enable

      - name: Install locked workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Bundle current arithmetic-only DS authority
        run: >-
          pnpm --dir artifacts/api-server exec esbuild
          src/quant-v4/topics/Arithmetic/subtopics/SimplificationAndApproximation/SAP-001/SAP-CP-006/authority-wave3-v3.test.ts
          --bundle --platform=node --format=esm
          --outfile=dist/quant-v4/sap-cp006-wave3-authority.test.mjs

      - name: Run current arithmetic-only DS authority
        shell: bash
        run: |
          set -o pipefail
          node artifacts/api-server/dist/quant-v4/sap-cp006-wave3-authority.test.mjs 2>&1 | tee artifacts/api-server/dist/quant-v4/sap-cp006-wave3-authority.log

      - name: Upload wave-three authority evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sap-cp006-wave3-authority
          path: artifacts/api-server/dist/quant-v4/sap-cp006-wave3-authority.log
          if-no-files-found: error
          retention-days: 30
