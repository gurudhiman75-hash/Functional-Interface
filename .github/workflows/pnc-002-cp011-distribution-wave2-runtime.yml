name: Validate PNC-002 CP-011 distribution wave 2

on:
  push:
    branches:
      - feat/pnc-002-cp011-grouping-distribution
    paths:
      - artifacts/api-server/src/quant-v4/topics/Arithmetic/subtopics/PermutationAndCombination/PNC-002/**
      - .github/workflows/pnc-002-cp011-distribution-wave2-runtime.yml
  pull_request:
    branches:
      - feat/pnc-002-cp010-circular-runtime-proof
    types: [opened, reopened, synchronize]
    paths:
      - artifacts/api-server/src/quant-v4/topics/Arithmetic/subtopics/PermutationAndCombination/PNC-002/**
      - .github/workflows/pnc-002-cp011-distribution-wave2-runtime.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validate-pnc-002-cp011-distribution-wave2-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-pnc-002-cp011-distribution-wave2:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Enable pnpm
        run: corepack enable
      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile
      - name: Strict TypeScript check
        run: |
          cd artifacts/api-server
          pnpm exec tsc \
            --noEmit \
            --target es2022 \
            --module esnext \
            --moduleResolution bundler \
            --resolveJsonModule \
            --allowSyntheticDefaultImports \
            --strictNullChecks \
            --noImplicitAny \
            --noImplicitReturns \
            --noFallthroughCasesInSwitch \
            --skipLibCheck \
            --types node \
            src/quant-v4/topics/Arithmetic/subtopics/PermutationAndCombination/PNC-002/foundation/cp011-distribution-wave2-runtime.ts \
            src/quant-v4/topics/Arithmetic/subtopics/PermutationAndCombination/PNC-002/pnc-002-cp011-distribution-wave2.test.ts
      - name: Bundle CP-011 distribution wave 2 runtime proof
        run: >-
          pnpm --dir artifacts/api-server exec esbuild
          src/quant-v4/topics/Arithmetic/subtopics/PermutationAndCombination/PNC-002/pnc-002-cp011-distribution-wave2.test.ts
          --bundle
          --platform=node
          --format=esm
          --outfile=dist/quant-v4/pnc-002-cp011-distribution-wave2-proof.mjs
      - name: Run CP-011 distribution wave 2 proof and generate review export
        working-directory: artifacts/api-server
        shell: bash
        run: |
          mkdir -p dist/quant-v4/pnc-002-cp011-distribution-wave2-diagnostics
          set +e
          node dist/quant-v4/pnc-002-cp011-distribution-wave2-proof.mjs 2>&1 | tee dist/quant-v4/pnc-002-cp011-distribution-wave2-diagnostics/runtime.log
          status=${PIPESTATUS[0]}
          exit "$status"
      - name: Publish CP-011 distribution wave 2 review files
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pnc-002-cp011-distribution-wave2-question-explanation-review
          path: artifacts/api-server/dist/quant-v4/pnc-002-cp011-distribution-wave2-review/
          if-no-files-found: warn
          retention-days: 14
      - name: Publish CP-011 distribution wave 2 diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pnc-002-cp011-distribution-wave2-diagnostics
          path: artifacts/api-server/dist/quant-v4/pnc-002-cp011-distribution-wave2-diagnostics/
          if-no-files-found: error
          retention-days: 3
