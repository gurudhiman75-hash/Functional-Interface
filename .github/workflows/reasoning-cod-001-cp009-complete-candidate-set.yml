name: Reasoning COD-001 CP-009 Complete Candidate Set

on:
  pull_request:
    paths:
      - "artifacts/api-server/src/reasoning-v1/topics/Coding-Decoding/COD-001/COD-CP-009/**"
      - ".github/workflows/reasoning-cod-001-cp009-complete-candidate-set.yml"
  workflow_dispatch:

jobs:
  cp009-complete-candidate-set:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Run complete candidate-set audit
        id: candidate_set_audit
        shell: bash
        run: |
          set +e
          npx --yes tsx artifacts/api-server/src/reasoning-v1/topics/Coding-Decoding/COD-001/COD-CP-009/complete-candidate-set-prototype.test.ts > cod-cp009-complete-candidate-set.log 2>&1
          status=$?
          echo "exit_code=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload complete candidate-set diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cod-001-cp009-complete-candidate-set-audit
          path: cod-cp009-complete-candidate-set.log
          retention-days: 7

      - name: Enforce complete candidate-set result
        shell: bash
        run: |
          cat cod-cp009-complete-candidate-set.log
          test "${{ steps.candidate_set_audit.outputs.exit_code }}" = "0"

      - name: Export complete candidate-set review pack
        run: npx --yes tsx artifacts/api-server/src/reasoning-v1/topics/Coding-Decoding/COD-001/COD-CP-009/export-complete-candidate-set-review.ts cod-cp009-complete-candidate-set-review-output

      - name: Upload complete candidate-set review pack
        uses: actions/upload-artifact@v4
        with:
          name: cod-001-cp009-complete-candidate-set-review
          path: cod-cp009-complete-candidate-set-review-output
          retention-days: 7
