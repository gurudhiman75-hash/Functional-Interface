name: Materialize NUM-CP-007 Wave 01 source

on:
  pull_request:
    branches:
      - New-main
    types: [opened, reopened, synchronize]
    paths:
      - .github/workflows/num-002-cp007-bootstrap.yml
      - .github/num-cp007-bootstrap/num-cp007-wave01-source.tar.gz
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: materialize-num-cp007-wave01-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  materialize:
    if: github.event_name == 'workflow_dispatch' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          fetch-depth: 0

      - name: Verify and extract tested source
        shell: bash
        run: |
          set -euo pipefail
          echo "94916f1445f643b3a7c1e130bec59a79489b2732c7b5b3624072d9f82dedac3e  .github/num-cp007-bootstrap/num-cp007-wave01-source.tar.gz" | sha256sum --check
          tar -xzf .github/num-cp007-bootstrap/num-cp007-wave01-source.tar.gz -C .
          rm -f .github/num-cp007-bootstrap/num-cp007-wave01-source.tar.gz
          rmdir .github/num-cp007-bootstrap || true

      - name: Commit materialized source
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "NUM-CP-007 Wave 01 source is already materialized."
            exit 0
          fi
          git commit -m "feat(num-cp007): materialize Wave 01 division foundation"
          git push origin "HEAD:${{ github.head_ref || github.ref_name }}"
