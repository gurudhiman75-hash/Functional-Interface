name: Validate SPA EMB-001 Admin Question Studio Route Activation V1

on:
  push:
    branches:
      - feat/spa-emb-001-question-studio-standard-v1
    paths:
      - artifacts/api-server/src/routes/admin-question-studio-spatial.ts
      - artifacts/api-server/src/reasoning-v1/tests/spatial-embedded-figure-admin-question-studio-route-activation-v1.test.ts
      - .github/workflows/spa-emb-001-admin-question-studio-route-activation-v1.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-emb-admin-question-studio-route-activation-v1:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: corepack enable

      - name: Install locked workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Build API server
        run: pnpm --dir artifacts/api-server build

      - name: Bundle EMB-001 admin route activation proof
        run: |
          mkdir -p artifacts/api-server/dist/reasoning-v1/spatial
          pnpm --dir artifacts/api-server exec esbuild \
            src/reasoning-v1/tests/spatial-embedded-figure-admin-question-studio-route-activation-v1.test.ts \
            --bundle --platform=node --format=esm \
            --outfile=dist/reasoning-v1/spatial/spatial-embedded-figure-admin-question-studio-route-activation-v1.test.mjs

      - name: Validate EMB-001 admin Question Studio route activation
        run: |
          cd artifacts/api-server
          node dist/reasoning-v1/spatial/spatial-embedded-figure-admin-question-studio-route-activation-v1.test.mjs \
            | tee dist/reasoning-v1/spatial/spa-emb-001-admin-question-studio-route-activation-v1-evidence.json

      - name: Upload EMB-001 admin route activation evidence
        uses: actions/upload-artifact@v4
        with:
          name: spa-emb-001-admin-question-studio-route-activation-v1
          path: artifacts/api-server/dist/reasoning-v1/spatial/spa-emb-001-admin-question-studio-route-activation-v1-evidence.json
          if-no-files-found: error
