name: Export Punjabi Retrofit CP007 Review

on:
  push:
    branches:
      - "feature/punjabi-retrofit-cp007-exhaustive-v1"
    paths:
      - "artifacts/api-server/src/punjabi-v1/**"
      - ".github/workflows/punjabi-forward-port-cp007-review.yml"
  pull_request:
    paths:
      - "artifacts/api-server/src/punjabi-v1/**"
      - ".github/workflows/punjabi-forward-port-cp007-review.yml"
  workflow_dispatch:

jobs:
  review-cp007-retrofit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with: {node-version: 22, cache: pnpm}
      - run: pnpm install --frozen-lockfile
      - name: Bundle CP007 retrofit review exporter
        working-directory: artifacts/api-server
        run: pnpm exec esbuild src/punjabi-v1/packages/PUN-001/checkpoints/CP007/export-review.ts --bundle --platform=node --format=esm --outfile=dist/punjabi-retrofit-cp007-review.mjs
      - name: Generate CP007 retrofit review pack
        working-directory: artifacts/api-server
        run: node dist/punjabi-retrofit-cp007-review.mjs
      - name: Upload CP007 retrofit review pack
        uses: actions/upload-artifact@v4
        with:
          name: PUN-001-CP007-RETROFIT-REVIEW-120
          path: artifacts/api-server/PUN-001-CP007-RETROFIT-REVIEW.md
          if-no-files-found: error
