name: Validate PNC-002 CP-011 grouping runtime

on:
  push:
    branches:
      - feat/pnc-002-cp011-grouping-distribution
    paths:
      - artifacts/api-server/src/quant-v4/topics/Arithmetic/subtopics/PermutationAndCombination/PNC-002/**
      - .github/workflows/pnc-002-cp011-grouping-runtime.yml
  pull_request:
    branches:
      - feat/pnc-002-cp010-circular-runtime-proof
    types: [opened, reopened, synchronize]
    paths:
      - artifacts/api-server/src/quant-v4/topics/Arithmetic/subtopics/PermutationAndCombination/PNC-002/**
      - .github/workflows/pnc-002-cp011-grouping-runtime.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validate-pnc-002-cp011-grouping-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-pnc-002-cp011-grouping:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Enable pnpm
        run: corepack enable
      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile
      - name: Strict TypeScript check
        run: |
          cd artifacts/api-server
          pnpm exec tsc \
            --noEmit \
            --target es2022 \
            --module esnext \
            --moduleResolution bundler \
            --resolveJsonModule \
            --allowSyntheticDefaultImports \
            --strictNullChecks \
            --noImplicitAny \
            --noImplicitReturns \
            --noFallthroughCasesInSwitch \
            --skipLibCheck \
            --types node \
            $(find src/quant-v4/topics/Arithmetic/subtopics/PermutationAndCombination/PNC-002 -name '*.ts' -print)
      - name: Bundle CP-011 grouping runtime proof
        run: >-
          pnpm --dir artifacts/api-server exec esbuild
          src/quant-v4/topics/Arithmetic/subtopics/PermutationAndCombination/PNC-002/pnc-002-cp011-grouping.test.ts
          --bundle
          --platform=node
          --format=esm
          --outfile=dist/quant-v4/pnc-002-cp011-grouping-proof.mjs
      - name: Run CP-011 grouping proof and generate review export
        working-directory: artifacts/api-server
        shell: bash
        run: |
          mkdir -p dist/quant-v4/pnc-002-cp011-grouping-diagnostics
          set +e
          node dist/quant-v4/pnc-002-cp011-grouping-proof.mjs 2>&1 | tee dist/quant-v4/pnc-002-cp011-grouping-diagnostics/runtime.log
          status=${PIPESTATUS[0]}
          exit "$status"
      - name: Publish CP-011 grouping review files
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pnc-002-cp011-grouping-question-explanation-review
          path: artifacts/api-server/dist/quant-v4/pnc-002-cp011-grouping-review/
          if-no-files-found: warn
          retention-days: 14
      - name: Publish CP-011 grouping diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pnc-002-cp011-grouping-diagnostics
          path: artifacts/api-server/dist/quant-v4/pnc-002-cp011-grouping-diagnostics/
          if-no-files-found: error
          retention-days: 3
