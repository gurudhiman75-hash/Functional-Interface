name: Validate Geometry strict 81-to-75 proposal

on:
  pull_request:
    branches:
      - New-main
    types: [opened, reopened, synchronize, ready_for_review]
    paths:
      - artifacts/api-server/src/quant-v4/topics/AdvancedMathematics/subtopics/Geometry/permanent-review/**
      - artifacts/api-server/src/quant-v4/topics/AdvancedMathematics/subtopics/Geometry/tests/geometry-merge-split-proposal-v1.test.ts
      - .github/workflows/geometry-phase5.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: geometry-strict-merge-split-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-geometry-81-to-75-proposal:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: corepack enable

      - name: Install locked workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Prove strict Geometry 81-to-75 merge split proposal
        run: |
          pnpm --dir artifacts/api-server exec esbuild src/quant-v4/topics/AdvancedMathematics/subtopics/Geometry/tests/geometry-merge-split-proposal-v1.test.ts --bundle --platform=node --format=esm --outfile=dist/quant-v4/geometry-merge-split-proposal-v1.test.mjs
          node artifacts/api-server/dist/quant-v4/geometry-merge-split-proposal-v1.test.mjs

      - name: Export strict Geometry 81-to-75 merge split proposal
        run: |
          pnpm --dir artifacts/api-server exec esbuild src/quant-v4/topics/AdvancedMathematics/subtopics/Geometry/permanent-review/export-geometry-merge-split-proposal-v1.ts --bundle --platform=node --format=esm --outfile=dist/quant-v4/geometry-merge-split-proposal-v1-export.mjs
          (cd artifacts/api-server && node dist/quant-v4/geometry-merge-split-proposal-v1-export.mjs)

      - name: Upload strict Geometry merge split proposal evidence
        uses: actions/upload-artifact@v4
        with:
          name: geometry-merge-split-proposal-v1
          path: artifacts/api-server/dist/quant-v4/geometry-merge-split-proposal-v1
          if-no-files-found: error
          retention-days: 30
