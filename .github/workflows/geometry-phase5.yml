name: Validate Geometry permanent 75 QL allocation

on:
  pull_request:
    branches:
      - New-main
    types: [opened, reopened, synchronize, ready_for_review]
    paths:
      - artifacts/api-server/src/quant-v4/topics/AdvancedMathematics/subtopics/Geometry/permanent-review/**
      - artifacts/api-server/src/quant-v4/topics/AdvancedMathematics/subtopics/Geometry/tests/geometry-permanent-ql-allocation-v1.test.ts
      - .github/workflows/geometry-phase5.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: geometry-permanent-ql-allocation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-geometry-permanent-75-ql-allocation:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: corepack enable

      - name: Install locked workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Prove Geometry permanent 75 QL allocation
        run: |
          pnpm --dir artifacts/api-server exec esbuild src/quant-v4/topics/AdvancedMathematics/subtopics/Geometry/tests/geometry-permanent-ql-allocation-v1.test.ts --bundle --platform=node --format=esm --outfile=dist/quant-v4/geometry-permanent-ql-allocation-v1.test.mjs
          node artifacts/api-server/dist/quant-v4/geometry-permanent-ql-allocation-v1.test.mjs

      - name: Export Geometry permanent 75 QL allocation evidence
        run: |
          pnpm --dir artifacts/api-server exec esbuild src/quant-v4/topics/AdvancedMathematics/subtopics/Geometry/permanent-review/export-geometry-permanent-ql-allocation-v1.ts --bundle --platform=node --format=esm --outfile=dist/quant-v4/geometry-permanent-ql-allocation-v1-export.mjs
          (cd artifacts/api-server && node dist/quant-v4/geometry-permanent-ql-allocation-v1-export.mjs)

      - name: Upload Geometry permanent 75 QL allocation evidence
        uses: actions/upload-artifact@v4
        with:
          name: geometry-permanent-ql-allocation-v1
          path: artifacts/api-server/dist/quant-v4/geometry-permanent-ql-allocation-v1
          if-no-files-found: error
          retention-days: 30
