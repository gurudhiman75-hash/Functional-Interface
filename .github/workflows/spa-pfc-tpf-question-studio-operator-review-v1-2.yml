name: Validate SPA PFC TPF Question Studio Operator Review V1.2

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-pfc-tpf-question-studio-operator-review-v1-2:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Enable pnpm
        run: corepack enable
      - name: Install locked workspace dependencies
        run: pnpm install --frozen-lockfile
      - name: Build API server
        run: pnpm --dir artifacts/api-server build
      - name: Bundle PFC TPF Studio operator review V1.2 gate
        run: |
          mkdir -p artifacts/api-server/dist/reasoning-v1/spatial
          pnpm --dir artifacts/api-server exec esbuild \
            src/reasoning-v1/tests/spatial-paper-folding-question-studio-operator-review-v1-2.test.ts \
            --bundle --platform=node --format=esm \
            --outfile=dist/reasoning-v1/spatial/spatial-paper-folding-question-studio-operator-review-v1-2.test.mjs
      - name: Validate varied PFC TPF Studio operator review
        run: cd artifacts/api-server && node dist/reasoning-v1/spatial/spatial-paper-folding-question-studio-operator-review-v1-2.test.mjs
      - name: Upload PFC TPF Studio operator review V1.2 artifact
        uses: actions/upload-artifact@v4
        with:
          name: spa-pfc-tpf-question-studio-operator-review-v1-2
          path: |
            artifacts/api-server/dist/reasoning-v1/spatial/spa-pfc-tpf-question-studio-operator-review-v1-2.html
            artifacts/api-server/dist/reasoning-v1/spatial/spa-pfc-tpf-question-studio-operator-review-v1-2-evidence.json
          if-no-files-found: error
