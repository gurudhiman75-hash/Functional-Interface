name: Validate TSD CP-004 Straight-Line Relative Motion

on:
  pull_request:
    paths:
      - 'artifacts/api-server/src/quant-v4/topics/Arithmetic/subtopics/TimeSpeedDistance/TSD-001/cp004/**'
      - 'artifacts/api-server/src/quant-v4/topics/Arithmetic/subtopics/TimeSpeedDistance/TSD-001/index.ts'
      - '.github/workflows/tsd-cp004-relative-motion.yml'
  push:
    branches:
      - feat/tsd-cp004-straight-line-relative-motion
    paths:
      - 'artifacts/api-server/src/quant-v4/topics/Arithmetic/subtopics/TimeSpeedDistance/TSD-001/cp004/**'
      - 'artifacts/api-server/src/quant-v4/topics/Arithmetic/subtopics/TimeSpeedDistance/TSD-001/index.ts'
      - '.github/workflows/tsd-cp004-relative-motion.yml'

jobs:
  validate-cp004:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Enable pnpm
        run: corepack enable

      - name: Install locked workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Preserve frozen CP003 public surface
        run: |
          INDEX=artifacts/api-server/src/quant-v4/topics/Arithmetic/subtopics/TimeSpeedDistance/TSD-001/index.ts
          grep -q 'cp003/localization/native-approved-freeze' "$INDEX"
          if grep -Eq 'cp003/localization/(native-final-candidate|native-final-polished-candidate)' "$INDEX"; then
            echo 'CP003 frozen public surface was reopened'
            exit 1
          fi

      - name: Bundle final CP004 proof and review exporter
        run: |
          ROOT=src/quant-v4/topics/Arithmetic/subtopics/TimeSpeedDistance/TSD-001/cp004
          OUT=dist/quant-v4/tsd-001/cp004-final-review
          mkdir -p artifacts/api-server/$OUT
          pnpm --dir artifacts/api-server exec esbuild "$ROOT/proof-final.ts" --bundle --platform=node --format=esm --outfile="$OUT/proof-final.mjs"
          pnpm --dir artifacts/api-server exec esbuild "$ROOT/export-review-final.ts" --bundle --platform=node --format=esm --outfile="$OUT/export-review-final.mjs"

      - name: Run final CP004 authority, verifier, pedagogy and localisation proofs
        run: |
          set -o pipefail
          cd artifacts/api-server
          OUT=dist/quant-v4/tsd-001/cp004-final-review
          node "$OUT/proof-final.mjs" 2>&1 | tee "$OUT/proof-final.log"
          node "$OUT/export-review-final.mjs" "$OUT/artifact" 2>&1 | tee "$OUT/export-final.log"

      - name: Build API server
        run: pnpm --dir artifacts/api-server build

      - name: Upload final CP004 review artifact
        uses: actions/upload-artifact@v4
        with:
          name: tsd-cp004-final-multilingual-review
          path: |
            artifacts/api-server/dist/quant-v4/tsd-001/cp004-final-review/artifact/*.json
            artifacts/api-server/dist/quant-v4/tsd-001/cp004-final-review/artifact/*.html
            artifacts/api-server/dist/quant-v4/tsd-001/cp004-final-review/*.log
          if-no-files-found: error
