name: Validate Render production build

on:
  push:
    branches:
      - New-main
    paths:
      - build-api.sh
      - package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - artifacts/admin-app/**
      - artifacts/examtree/**
      - artifacts/api-server/**
      - scripts/assemble-hosting.mjs
      - .github/workflows/render-production-build.yml
  pull_request:
    branches:
      - New-main
    types: [opened, reopened, synchronize]
    paths:
      - build-api.sh
      - package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - artifacts/admin-app/**
      - artifacts/examtree/**
      - artifacts/api-server/package.json
      - artifacts/api-server/tsconfig*.json
      - artifacts/api-server/src/app.ts
      - artifacts/api-server/src/index.ts
      - artifacts/api-server/src/db-*.ts
      - artifacts/api-server/src/content-quality/**
      - artifacts/api-server/src/current-affairs/**
      - artifacts/api-server/src/domain/**
      - artifacts/api-server/src/generators/**
      - artifacts/api-server/src/english-v1/chapters/reading-comprehension/ENG-008/**
      - artifacts/api-server/src/legacy-percentage-engine/**
      - artifacts/api-server/src/lib/**
      - artifacts/api-server/src/middlewares/**
      - artifacts/api-server/src/notes-studio/**
      - artifacts/api-server/src/notes-studio-v2/**
      - artifacts/api-server/src/routes/**
      - artifacts/api-server/src/server/**
      - artifacts/api-server/src/services/**
      - artifacts/api-server/src/types/**
      - artifacts/api-server/src/utils/**
      - artifacts/api-server/src/question-studio-review-registry.ts
      - scripts/assemble-hosting.mjs
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: render-production-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  render-production-build:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Run exact Render build command
        id: render_build
        shell: bash
        run: |
          set +e
          bash build-api.sh > render-production-build.log 2>&1
          status=$?
          echo "exit_code=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload Render build diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: render-production-build-diagnostics
          path: render-production-build.log
          retention-days: 7

      - name: Enforce Render build result
        shell: bash
        run: |
          cat render-production-build.log
          test "${{ steps.render_build.outputs.exit_code }}" = "0"

      - name: Verify deployment artifacts
        shell: bash
        run: |
          test -f artifacts/api-server/dist/index.mjs
          test -f artifacts/examtree/dist/public/index.html
          test -f artifacts/examtree/dist/public/admin/index.html
