name: COM-003 English Freeze V2

on:
  push:
    branches:
      - feature/com003-office-productivity-discovery-v1
    paths:
      - 'artifacts/api-server/src/knowledge-v1/computer-awareness/com003-english-freeze-v2.ts'
      - 'artifacts/api-server/src/knowledge-v1/computer-awareness/com003-english-freeze-v2.test.ts'
      - '.github/workflows/com003-english-freeze-v2.yml'
  pull_request:
    branches:
      - integration/computer-content-engine-mainline-v1
    paths:
      - 'artifacts/api-server/src/knowledge-v1/computer-awareness/com003-english-freeze-v2.ts'
      - 'artifacts/api-server/src/knowledge-v1/computer-awareness/com003-english-freeze-v2.test.ts'
      - '.github/workflows/com003-english-freeze-v2.yml'

permissions:
  contents: read

concurrency:
  group: com003-english-freeze-v2-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Enable pnpm
        run: corepack enable
      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile
      - name: Bundle English Freeze V2 audit
        run: |
          mkdir -p artifacts/api-server/dist/com003-english-freeze-v2
          pnpm --dir artifacts/api-server exec esbuild \
            src/knowledge-v1/computer-awareness/com003-english-freeze-v2.test.ts \
            --bundle --platform=node --format=esm \
            --outfile=dist/com003-english-freeze-v2/audit.test.mjs
      - name: Run English Freeze V2 audit
        run: node artifacts/api-server/dist/com003-english-freeze-v2/audit.test.mjs
