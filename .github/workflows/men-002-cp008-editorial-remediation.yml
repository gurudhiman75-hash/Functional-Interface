name: Validate MEN-CP-008 editorial remediation

on:
  pull_request:
    branches:
      - New-main
    types: [opened, reopened, synchronize]
    paths:
      - artifacts/api-server/src/quant-v4/topics/AdvancedMathematics/subtopics/Mensuration/MEN-002/MEN-CP-008/permanent/**
      - .github/workflows/men-002-cp008-editorial-remediation.yml
      - build-api.sh
      - package.json
      - pnpm-lock.yaml

permissions:
  contents: read

concurrency:
  group: validate-men-002-cp008-editorial-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-editorial-remediation:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: corepack enable

      - name: Install locked workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Prove all permanent CP-008 English packages
        shell: bash
        run: |
          set -euo pipefail
          pnpm --dir artifacts/api-server exec esbuild \
            src/quant-v4/topics/AdvancedMathematics/subtopics/Mensuration/MEN-002/MEN-CP-008/permanent/english.test.ts \
            --bundle \
            --packages=external \
            --platform=node \
            --format=esm \
            --outfile=dist/quant-v4/men-cp008-editorial-remediation.test.mjs
          node artifacts/api-server/dist/quant-v4/men-cp008-editorial-remediation.test.mjs

      - name: Build production API bundle
        shell: bash
        run: bash build-api.sh
