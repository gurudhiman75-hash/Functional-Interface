name: Validate platform LaTeX rendering

on:
  push:
    branches:
      - feat/platform-latex-rendering-pnc001
  pull_request:
    branches:
      - audit/pnc-001-package-freeze-readiness
    types: [opened, reopened, synchronize]

permissions:
  contents: read

concurrency:
  group: validate-platform-latex-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: corepack enable

      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck PNC-001 LaTeX runtime
        run: |
          cd artifacts/api-server
          pnpm exec tsc \
            --noEmit \
            --target es2022 \
            --module esnext \
            --moduleResolution bundler \
            --resolveJsonModule \
            --allowSyntheticDefaultImports \
            --strictNullChecks \
            --noImplicitAny \
            --noImplicitReturns \
            --noFallthroughCasesInSwitch \
            --skipLibCheck \
            --types node \
            $(find src/quant-v4/topics/Arithmetic/subtopics/PermutationAndCombination/PNC-001 -name '*.ts' -print)

      - name: Bundle rendered LaTeX corpus audit
        run: >-
          pnpm --dir artifacts/api-server exec esbuild
          src/quant-v4/topics/Arithmetic/subtopics/PermutationAndCombination/PNC-001/pnc-001-latex-audit.ts
          --bundle
          --platform=node
          --format=esm
          --outfile=dist/quant-v4/pnc-001-latex-audit.mjs

      - name: Audit all rendered PNC-001 formulas
        working-directory: artifacts/api-server
        run: node dist/quant-v4/pnc-001-latex-audit.mjs

      - name: Bundle PNC-001 proof
        run: >-
          pnpm --dir artifacts/api-server exec esbuild
          src/quant-v4/topics/Arithmetic/subtopics/PermutationAndCombination/PNC-001/pnc-001.test.ts
          --bundle
          --platform=node
          --format=esm
          --outfile=dist/quant-v4/pnc-001-latex-proof.mjs

      - name: Run PNC-001 proof
        working-directory: artifacts/api-server
        run: |
          if ! node dist/quant-v4/pnc-001-latex-proof.mjs > /tmp/pnc-proof.log 2>&1; then
            echo "PNC proof failed; compact diagnostic follows:"
            tail -n 100 /tmp/pnc-proof.log
            exit 1
          fi
          cat /tmp/pnc-proof.log

      - name: Publish PNC proof diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pnc-001-latex-proof-diagnostics
          path: /tmp/pnc-proof.log
          if-no-files-found: error
          retention-days: 7

      - name: Bundle final reviewed package audit
        run: >-
          pnpm --dir artifacts/api-server exec esbuild
          src/quant-v4/topics/Arithmetic/subtopics/PermutationAndCombination/PNC-001/pnc-001-package-audit-final.ts
          --bundle
          --platform=node
          --format=esm
          --outfile=dist/quant-v4/pnc-001-package-audit.mjs

      - name: Run final package audit
        working-directory: artifacts/api-server
        run: |
          if ! node dist/quant-v4/pnc-001-package-audit.mjs > /tmp/pnc-package-audit.log 2>&1; then
            echo "PNC package audit failed; compact diagnostic follows:"
            tail -n 120 /tmp/pnc-package-audit.log
            exit 1
          fi
          cat /tmp/pnc-package-audit.log

      - name: Publish package-audit diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pnc-001-latex-package-audit-diagnostics
          path: /tmp/pnc-package-audit.log
          if-no-files-found: warn
          retention-days: 7

      - name: Publish LaTeX package audit evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pnc-001-latex-package-audit
          path: artifacts/api-server/dist/quant-v4/pnc-001-package-audit/
          if-no-files-found: warn
          retention-days: 14

      - name: Typecheck admin application
        run: pnpm --dir artifacts/admin-app typecheck

      - name: Test admin application
        run: pnpm --dir artifacts/admin-app test

      - name: Build admin application
        run: pnpm --dir artifacts/admin-app build

      - name: Build student application
        run: pnpm --dir artifacts/examtree build
