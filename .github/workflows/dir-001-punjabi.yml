name: Validate DIR-001 Punjabi localization

on:
  pull_request:
    branches:
      - feat/reasoning-dir-001-design
    types: [opened, reopened, synchronize]
    paths:
      - artifacts/api-server/src/reasoning-v1/topics/Direction-Sense/DIR-001/**
      - .github/workflows/dir-001-punjabi.yml
  push:
    branches:
      - feat/dir-001-punjabi-localization
    paths:
      - artifacts/api-server/src/reasoning-v1/topics/Direction-Sense/DIR-001/**
      - .github/workflows/dir-001-punjabi.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validate-dir-001-punjabi-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-dir-001-punjabi:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Enable pnpm
        run: corepack enable
      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile
      - name: Typecheck DIR-001 Punjabi localization
        run: |
          set +e
          cd artifacts/api-server
          pnpm exec tsc --noEmit --target es2022 --module esnext --moduleResolution bundler --allowSyntheticDefaultImports --strictNullChecks --noImplicitAny --noImplicitReturns --noFallthroughCasesInSwitch --skipLibCheck --types node $(find src/reasoning-v1/topics/Direction-Sense/DIR-001 -name '*.ts' -print) > ../../dir-001-punjabi-typecheck.log 2>&1
          status="$?"
          cd ../..
          echo "$status" > dir-001-punjabi-typecheck-exit.txt
          cat dir-001-punjabi-typecheck.log
          exit 0
      - name: Publish Punjabi typecheck diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dir-001-punjabi-typecheck-diagnostics
          path: |
            dir-001-punjabi-typecheck.log
            dir-001-punjabi-typecheck-exit.txt
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Punjabi typecheck result
        run: test "$(cat dir-001-punjabi-typecheck-exit.txt)" = "0"
      - name: Bundle Punjabi proof
        run: >-
          pnpm --dir artifacts/api-server exec esbuild
          src/reasoning-v1/topics/Direction-Sense/DIR-001/dir-001-punjabi.test.ts
          --bundle --platform=node --format=esm
          --outfile=dist/reasoning-v1/dir-001-punjabi.test.mjs
      - name: Run Punjabi proof
        run: |
          set +e
          node artifacts/api-server/dist/reasoning-v1/dir-001-punjabi.test.mjs > dir-001-punjabi-proof.log 2>&1
          echo "$?" > dir-001-punjabi-proof-exit.txt
          cat dir-001-punjabi-proof.log
          exit 0
      - name: Publish Punjabi proof diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dir-001-punjabi-proof-diagnostics
          path: |
            dir-001-punjabi-proof.log
            dir-001-punjabi-proof-exit.txt
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Punjabi proof result
        run: test "$(cat dir-001-punjabi-proof-exit.txt)" = "0"
      - name: Bundle Punjabi review exporter
        run: >-
          pnpm --dir artifacts/api-server exec esbuild
          src/reasoning-v1/topics/Direction-Sense/DIR-001/dir-001-punjabi-export.ts
          --bundle --platform=node --format=esm
          --outfile=dist/reasoning-v1/dir-001-punjabi-export.mjs
      - name: Generate Punjabi review files
        working-directory: artifacts/api-server
        run: node dist/reasoning-v1/dir-001-punjabi-export.mjs
      - name: Publish Punjabi review files
        uses: actions/upload-artifact@v4
        with:
          name: dir-001-punjabi-question-review
          path: artifacts/api-server/dist/reasoning-v1/dir-001-punjabi-review/
          if-no-files-found: error
          retention-days: 21
